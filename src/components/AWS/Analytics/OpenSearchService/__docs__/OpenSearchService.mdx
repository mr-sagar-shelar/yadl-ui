import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as OpenSearchService from "./OpenSearchService.stories.tsx";

<Meta of={OpenSearchService} title="AWS/Analytics/OpenSearchService" />

# OpenSearchService

OpenSearchService component with different props.

#### Example

<Canvas of={OpenSearchService.Primary} />

## Usage

```ts
import { OpenSearchService } from "yadl-ui";

const Example = () => {
  return (
      <OpenSearchService />
  );
};

export default Example;
```

#### Credits
[AWS/Analytics](https://aws-icons.com/)
