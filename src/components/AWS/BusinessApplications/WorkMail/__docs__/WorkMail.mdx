import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as WorkMail from "./WorkMail.stories.tsx";

<Meta of={WorkMail} title="AWS/BusinessApplications/WorkMail" />

# WorkMail

WorkMail component with different props.

#### Example

<Canvas of={WorkMail.Primary} />

## Usage

```ts
import { WorkMail } from "yadl-ui";

const Example = () => {
  return (
      <WorkMail />
  );
};

export default Example;
```

#### Credits
[AWS/BusinessApplications](https://aws-icons.com/)
