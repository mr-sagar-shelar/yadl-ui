import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Monitron from "./Monitron.stories.tsx";

<Meta of={Monitron} title="AWS/MachineLearning/Monitron" />

# Monitron

Monitron component with different props.

#### Example

<Canvas of={Monitron.Primary} />

## Usage

```ts
import { Monitron } from "yadl-ui";

const Example = () => {
  return (
      <Monitron />
  );
};

export default Example;
```

#### Credits
[AWS/MachineLearning](https://aws-icons.com/)
