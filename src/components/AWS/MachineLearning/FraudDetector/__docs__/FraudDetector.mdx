import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as FraudDetector from "./FraudDetector.stories.tsx";

<Meta of={FraudDetector} title="AWS/MachineLearning/FraudDetector" />

# FraudDetector

FraudDetector component with different props.

#### Example

<Canvas of={FraudDetector.Primary} />

## Usage

```ts
import { FraudDetector } from "yadl-ui";

const Example = () => {
  return (
      <FraudDetector />
  );
};

export default Example;
```

#### Credits
[AWS/MachineLearning](https://aws-icons.com/)
