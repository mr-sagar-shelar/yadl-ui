import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as TorchServe from "./TorchServe.stories.tsx";

<Meta of={TorchServe} title="AWS/MachineLearning/TorchServe" />

# TorchServe

TorchServe component with different props.

#### Example

<Canvas of={TorchServe.Primary} />

## Usage

```ts
import { TorchServe } from "yadl-ui";

const Example = () => {
  return (
      <TorchServe />
  );
};

export default Example;
```

#### Credits
[AWS/MachineLearning](https://aws-icons.com/)
