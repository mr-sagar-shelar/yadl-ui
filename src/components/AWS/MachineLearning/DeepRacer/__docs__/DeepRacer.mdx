import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DeepRacer from "./DeepRacer.stories.tsx";

<Meta of={DeepRacer} title="AWS/MachineLearning/DeepRacer" />

# DeepRacer

DeepRacer component with different props.

#### Example

<Canvas of={DeepRacer.Primary} />

## Usage

```ts
import { DeepRacer } from "yadl-ui";

const Example = () => {
  return (
      <DeepRacer />
  );
};

export default Example;
```

#### Credits
[AWS/MachineLearning](https://aws-icons.com/)
