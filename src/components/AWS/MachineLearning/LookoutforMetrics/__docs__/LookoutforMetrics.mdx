import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as LookoutforMetrics from "./LookoutforMetrics.stories.tsx";

<Meta of={LookoutforMetrics} title="AWS/MachineLearning/LookoutforMetrics" />

# LookoutforMetrics

LookoutforMetrics component with different props.

#### Example

<Canvas of={LookoutforMetrics.Primary} />

## Usage

```ts
import { LookoutforMetrics } from "yadl-ui";

const Example = () => {
  return (
      <LookoutforMetrics />
  );
};

export default Example;
```

#### Credits
[AWS/MachineLearning](https://aws-icons.com/)
