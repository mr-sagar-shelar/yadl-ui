import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Bottlerocket from "./Bottlerocket.stories.tsx";

<Meta of={Bottlerocket} title="AWS/Compute/Bottlerocket" />

# Bottlerocket

Bottlerocket component with different props.

#### Example

<Canvas of={Bottlerocket.Primary} />

## Usage

```ts
import { Bottlerocket } from "yadl-ui";

const Example = () => {
  return (
      <Bottlerocket />
  );
};

export default Example;
```

#### Credits
[AWS/Compute](https://aws-icons.com/)
