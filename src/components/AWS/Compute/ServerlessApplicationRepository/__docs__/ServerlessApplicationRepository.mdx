import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ServerlessApplicationRepository from "./ServerlessApplicationRepository.stories.tsx";

<Meta of={ServerlessApplicationRepository} title="AWS/Compute/ServerlessApplicationRepository" />

# ServerlessApplicationRepository

ServerlessApplicationRepository component with different props.

#### Example

<Canvas of={ServerlessApplicationRepository.Primary} />

## Usage

```ts
import { ServerlessApplicationRepository } from "yadl-ui";

const Example = () => {
  return (
      <ServerlessApplicationRepository />
  );
};

export default Example;
```

#### Credits
[AWS/Compute](https://aws-icons.com/)
