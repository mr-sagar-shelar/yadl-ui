import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ThinkboxXMesh from "./ThinkboxXMesh.stories.tsx";

<Meta of={ThinkboxXMesh} title="AWS/Compute/ThinkboxXMesh" />

# ThinkboxXMesh

ThinkboxXMesh component with different props.

#### Example

<Canvas of={ThinkboxXMesh.Primary} />

## Usage

```ts
import { ThinkboxXMesh } from "yadl-ui";

const Example = () => {
  return (
      <ThinkboxXMesh />
  );
};

export default Example;
```

#### Credits
[AWS/Compute](https://aws-icons.com/)
