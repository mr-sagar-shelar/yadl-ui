import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ThinkboxStoke from "./ThinkboxStoke.stories.tsx";

<Meta of={ThinkboxStoke} title="AWS/Compute/ThinkboxStoke" />

# ThinkboxStoke

ThinkboxStoke component with different props.

#### Example

<Canvas of={ThinkboxStoke.Primary} />

## Usage

```ts
import { ThinkboxStoke } from "yadl-ui";

const Example = () => {
  return (
      <ThinkboxStoke />
  );
};

export default Example;
```

#### Credits
[AWS/Compute](https://aws-icons.com/)
