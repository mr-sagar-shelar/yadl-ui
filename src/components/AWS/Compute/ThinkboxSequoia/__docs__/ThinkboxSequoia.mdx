import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ThinkboxSequoia from "./ThinkboxSequoia.stories.tsx";

<Meta of={ThinkboxSequoia} title="AWS/Compute/ThinkboxSequoia" />

# ThinkboxSequoia

ThinkboxSequoia component with different props.

#### Example

<Canvas of={ThinkboxSequoia.Primary} />

## Usage

```ts
import { ThinkboxSequoia } from "yadl-ui";

const Example = () => {
  return (
      <ThinkboxSequoia />
  );
};

export default Example;
```

#### Credits
[AWS/Compute](https://aws-icons.com/)
