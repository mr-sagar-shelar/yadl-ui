import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AppStream from "./AppStream.stories.tsx";

<Meta of={AppStream} title="AWS/EndUserComputing/AppStream" />

# AppStream

AppStream component with different props.

#### Example

<Canvas of={AppStream.Primary} />

## Usage

```ts
import { AppStream } from "yadl-ui";

const Example = () => {
  return (
      <AppStream />
  );
};

export default Example;
```

#### Credits
[AWS/EndUserComputing](https://aws-icons.com/)
