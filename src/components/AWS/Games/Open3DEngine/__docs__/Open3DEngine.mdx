import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Open3DEngine from "./Open3DEngine.stories.tsx";

<Meta of={Open3DEngine} title="AWS/Games/Open3DEngine" />

# Open3DEngine

Open3DEngine component with different props.

#### Example

<Canvas of={Open3DEngine.Primary} />

## Usage

```ts
import { Open3DEngine } from "yadl-ui";

const Example = () => {
  return (
      <Open3DEngine />
  );
};

export default Example;
```

#### Credits
[AWS/Games](https://aws-icons.com/)
