import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudDirectory from "./CloudDirectory.stories.tsx";

<Meta of={CloudDirectory} title="AWS/SecurityIdentityCompliance/CloudDirectory" />

# CloudDirectory

CloudDirectory component with different props.

#### Example

<Canvas of={CloudDirectory.Primary} />

## Usage

```ts
import { CloudDirectory } from "yadl-ui";

const Example = () => {
  return (
      <CloudDirectory />
  );
};

export default Example;
```

#### Credits
[AWS/SecurityIdentityCompliance](https://aws-icons.com/)
