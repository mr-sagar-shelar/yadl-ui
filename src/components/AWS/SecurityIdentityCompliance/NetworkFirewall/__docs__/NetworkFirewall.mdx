import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as NetworkFirewall from "./NetworkFirewall.stories.tsx";

<Meta of={NetworkFirewall} title="AWS/SecurityIdentityCompliance/NetworkFirewall" />

# NetworkFirewall

NetworkFirewall component with different props.

#### Example

<Canvas of={NetworkFirewall.Primary} />

## Usage

```ts
import { NetworkFirewall } from "yadl-ui";

const Example = () => {
  return (
      <NetworkFirewall />
  );
};

export default Example;
```

#### Credits
[AWS/SecurityIdentityCompliance](https://aws-icons.com/)
