import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ResourceAccessManager from "./ResourceAccessManager.stories.tsx";

<Meta of={ResourceAccessManager} title="AWS/SecurityIdentityCompliance/ResourceAccessManager" />

# ResourceAccessManager

ResourceAccessManager component with different props.

#### Example

<Canvas of={ResourceAccessManager.Primary} />

## Usage

```ts
import { ResourceAccessManager } from "yadl-ui";

const Example = () => {
  return (
      <ResourceAccessManager />
  );
};

export default Example;
```

#### Credits
[AWS/SecurityIdentityCompliance](https://aws-icons.com/)
