import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as FSxforWFS from "./FSxforWFS.stories.tsx";

<Meta of={FSxforWFS} title="AWS/Storage/FSxforWFS" />

# FSxforWFS

FSxforWFS component with different props.

#### Example

<Canvas of={FSxforWFS.Primary} />

## Usage

```ts
import { FSxforWFS } from "yadl-ui";

const Example = () => {
  return (
      <FSxforWFS />
  );
};

export default Example;
```

#### Credits
[AWS/Storage](https://aws-icons.com/)
