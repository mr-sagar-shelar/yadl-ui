import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ElementalServer from "./ElementalServer.stories.tsx";

<Meta of={ElementalServer} title="AWS/MediaServices/ElementalServer" />

# ElementalServer

ElementalServer component with different props.

#### Example

<Canvas of={ElementalServer.Primary} />

## Usage

```ts
import { ElementalServer } from "yadl-ui";

const Example = () => {
  return (
      <ElementalServer />
  );
};

export default Example;
```

#### Credits
[AWS/MediaServices](https://aws-icons.com/)
