import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ServiceCatalog from "./ServiceCatalog.stories.tsx";

<Meta of={ServiceCatalog} title="AWS/ManagementGovernance/ServiceCatalog" />

# ServiceCatalog

ServiceCatalog component with different props.

#### Example

<Canvas of={ServiceCatalog.Primary} />

## Usage

```ts
import { ServiceCatalog } from "yadl-ui";

const Example = () => {
  return (
      <ServiceCatalog />
  );
};

export default Example;
```

#### Credits
[AWS/ManagementGovernance](https://aws-icons.com/)
