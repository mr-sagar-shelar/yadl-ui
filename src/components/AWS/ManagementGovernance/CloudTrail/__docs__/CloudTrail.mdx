import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudTrail from "./CloudTrail.stories.tsx";

<Meta of={CloudTrail} title="AWS/ManagementGovernance/CloudTrail" />

# CloudTrail

CloudTrail component with different props.

#### Example

<Canvas of={CloudTrail.Primary} />

## Usage

```ts
import { CloudTrail } from "yadl-ui";

const Example = () => {
  return (
      <CloudTrail />
  );
};

export default Example;
```

#### Credits
[AWS/ManagementGovernance](https://aws-icons.com/)
