import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as LaunchWizard from "./LaunchWizard.stories.tsx";

<Meta of={LaunchWizard} title="AWS/ManagementGovernance/LaunchWizard" />

# LaunchWizard

LaunchWizard component with different props.

#### Example

<Canvas of={LaunchWizard.Primary} />

## Usage

```ts
import { LaunchWizard } from "yadl-ui";

const Example = () => {
  return (
      <LaunchWizard />
  );
};

export default Example;
```

#### Credits
[AWS/ManagementGovernance](https://aws-icons.com/)
