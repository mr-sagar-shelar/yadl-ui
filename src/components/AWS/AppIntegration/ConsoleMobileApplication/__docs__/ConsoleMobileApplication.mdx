import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ConsoleMobileApplication from "./ConsoleMobileApplication.stories.tsx";

<Meta of={ConsoleMobileApplication} title="AWS/AppIntegration/ConsoleMobileApplication" />

# ConsoleMobileApplication

ConsoleMobileApplication component with different props.

#### Example

<Canvas of={ConsoleMobileApplication.Primary} />

## Usage

```ts
import { ConsoleMobileApplication } from "yadl-ui";

const Example = () => {
  return (
      <ConsoleMobileApplication />
  );
};

export default Example;
```

#### Credits
[AWS/AppIntegration](https://aws-icons.com/)
