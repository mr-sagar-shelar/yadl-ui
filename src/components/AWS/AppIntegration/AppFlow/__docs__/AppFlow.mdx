import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AppFlow from "./AppFlow.stories.tsx";

<Meta of={AppFlow} title="AWS/AppIntegration/AppFlow" />

# AppFlow

AppFlow component with different props.

#### Example

<Canvas of={AppFlow.Primary} />

## Usage

```ts
import { AppFlow } from "yadl-ui";

const Example = () => {
  return (
      <AppFlow />
  );
};

export default Example;
```

#### Credits
[AWS/AppIntegration](https://aws-icons.com/)
