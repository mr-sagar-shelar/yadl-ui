import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AppSync from "./AppSync.stories.tsx";

<Meta of={AppSync} title="AWS/AppIntegration/AppSync" />

# AppSync

AppSync component with different props.

#### Example

<Canvas of={AppSync.Primary} />

## Usage

```ts
import { AppSync } from "yadl-ui";

const Example = () => {
  return (
      <AppSync />
  );
};

export default Example;
```

#### Credits
[AWS/AppIntegration](https://aws-icons.com/)
