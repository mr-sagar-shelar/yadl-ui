import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ManagedBlockchain from "./ManagedBlockchain.stories.tsx";

<Meta of={ManagedBlockchain} title="AWS/Blockchain/ManagedBlockchain" />

# ManagedBlockchain

ManagedBlockchain component with different props.

#### Example

<Canvas of={ManagedBlockchain.Primary} />

## Usage

```ts
import { ManagedBlockchain } from "yadl-ui";

const Example = () => {
  return (
      <ManagedBlockchain />
  );
};

export default Example;
```

#### Credits
[AWS/Blockchain](https://aws-icons.com/)
