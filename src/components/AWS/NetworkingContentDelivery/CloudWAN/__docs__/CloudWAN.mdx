import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudWAN from "./CloudWAN.stories.tsx";

<Meta of={CloudWAN} title="AWS/NetworkingContentDelivery/CloudWAN" />

# CloudWAN

CloudWAN component with different props.

#### Example

<Canvas of={CloudWAN.Primary} />

## Usage

```ts
import { CloudWAN } from "yadl-ui";

const Example = () => {
  return (
      <CloudWAN />
  );
};

export default Example;
```

#### Credits
[AWS/NetworkingContentDelivery](https://aws-icons.com/)
