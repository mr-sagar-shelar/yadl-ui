import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DirectConnect from "./DirectConnect.stories.tsx";

<Meta of={DirectConnect} title="AWS/NetworkingContentDelivery/DirectConnect" />

# DirectConnect

DirectConnect component with different props.

#### Example

<Canvas of={DirectConnect.Primary} />

## Usage

```ts
import { DirectConnect } from "yadl-ui";

const Example = () => {
  return (
      <DirectConnect />
  );
};

export default Example;
```

#### Credits
[AWS/NetworkingContentDelivery](https://aws-icons.com/)
