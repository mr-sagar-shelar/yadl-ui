import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ElasticKubernetesService from "./ElasticKubernetesService.stories.tsx";

<Meta of={ElasticKubernetesService} title="AWS/Containers/ElasticKubernetesService" />

# ElasticKubernetesService

ElasticKubernetesService component with different props.

#### Example

<Canvas of={ElasticKubernetesService.Primary} />

## Usage

```ts
import { ElasticKubernetesService } from "yadl-ui";

const Example = () => {
  return (
      <ElasticKubernetesService />
  );
};

export default Example;
```

#### Credits
[AWS/Containers](https://aws-icons.com/)
