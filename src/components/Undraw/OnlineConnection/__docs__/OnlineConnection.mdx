import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as OnlineConnection from "./OnlineConnection.stories.tsx";

<Meta of={OnlineConnection} title="unDraw/OnlineConnection" />

# OnlineConnection

OnlineConnection component with different props.

#### Example

<Canvas of={OnlineConnection.Primary} />

## Usage

```ts
import { OnlineConnection } from "yadl-ui";

const Example = () => {
  return (
      <OnlineConnection />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
