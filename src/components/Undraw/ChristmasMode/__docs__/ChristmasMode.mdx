import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ChristmasMode from "./ChristmasMode.stories.tsx";

<Meta of={ChristmasMode} title="unDraw/ChristmasMode" />

# ChristmasMode

ChristmasMode component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={ChristmasMode.Primary} />

## Usage

```ts
import { ChristmasMode } from "yadl-ui";

const Example = () => {
  return (
      <ChristmasMode />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
