import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as IntenseFeelingIntenseFeeling from "./IntenseFeelingIntenseFeeling.stories.tsx";

<Meta of={IntenseFeelingIntenseFeeling} title="unDraw/IntenseFeelingIntenseFeeling" />

# IntenseFeelingIntenseFeeling

IntenseFeelingIntenseFeeling component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={IntenseFeelingIntenseFeeling.Primary} />

## Usage

```ts
import { IntenseFeelingIntenseFeeling } from "yadl-ui";

const Example = () => {
  return (
      <IntenseFeelingIntenseFeeling />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
