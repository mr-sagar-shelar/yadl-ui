import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as WatchApplication from "./WatchApplication.stories.tsx";

<Meta of={WatchApplication} title="unDraw/WatchApplication" />

# WatchApplication

WatchApplication component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={WatchApplication.Primary} />

## Usage

```ts
import { WatchApplication } from "yadl-ui";

const Example = () => {
  return (
      <WatchApplication />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
