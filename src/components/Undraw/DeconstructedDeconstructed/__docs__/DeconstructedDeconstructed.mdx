import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DeconstructedDeconstructed from "./DeconstructedDeconstructed.stories.tsx";

<Meta of={DeconstructedDeconstructed} title="unDraw/DeconstructedDeconstructed" />

# DeconstructedDeconstructed

DeconstructedDeconstructed component with different props.

#### Example

<Canvas of={DeconstructedDeconstructed.Primary} />

## Usage

```ts
import { DeconstructedDeconstructed } from "yadl-ui";

const Example = () => {
  return (
      <DeconstructedDeconstructed />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
