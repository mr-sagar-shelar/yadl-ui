import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as OptimizeImage from "./OptimizeImage.stories.tsx";

<Meta of={OptimizeImage} title="unDraw/OptimizeImage" />

# OptimizeImage

OptimizeImage component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={OptimizeImage.Primary} />

## Usage

```ts
import { OptimizeImage } from "yadl-ui";

const Example = () => {
  return (
      <OptimizeImage />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
