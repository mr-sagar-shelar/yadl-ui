import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Zoom_inZoomIn from "./Zoom_inZoomIn.stories.tsx";

<Meta of={Zoom_inZoomIn} title="unDraw/Zoom_inZoomIn" />

# Zoom_inZoomIn

Zoom_inZoomIn component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Zoom_inZoomIn.Primary} />

## Usage

```ts
import { Zoom_inZoomIn } from "yadl-ui";

const Example = () => {
  return (
      <Zoom_inZoomIn />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
