import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ReadingTime from "./ReadingTime.stories.tsx";

<Meta of={ReadingTime} title="unDraw/ReadingTime" />

# ReadingTime

ReadingTime component with different props.

#### Example

<Canvas of={ReadingTime.Primary} />

## Usage

```ts
import { ReadingTime } from "yadl-ui";

const Example = () => {
  return (
      <ReadingTime />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
