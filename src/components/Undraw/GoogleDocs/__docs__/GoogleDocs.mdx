import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as GoogleDocs from "./GoogleDocs.stories.tsx";

<Meta of={GoogleDocs} title="unDraw/GoogleDocs" />

# GoogleDocs

GoogleDocs component with different props.

#### Example

<Canvas of={GoogleDocs.Primary} />

## Usage

```ts
import { GoogleDocs } from "yadl-ui";

const Example = () => {
  return (
      <GoogleDocs />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
