import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as SecureFiles from "./SecureFiles.stories.tsx";

<Meta of={SecureFiles} title="unDraw/SecureFiles" />

# SecureFiles

SecureFiles component with different props.

#### Example

<Canvas of={SecureFiles.Primary} />

## Usage

```ts
import { SecureFiles } from "yadl-ui";

const Example = () => {
  return (
      <SecureFiles />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
