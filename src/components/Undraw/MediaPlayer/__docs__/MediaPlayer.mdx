import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as MediaPlayer from "./MediaPlayer.stories.tsx";

<Meta of={MediaPlayer} title="unDraw/MediaPlayer" />

# MediaPlayer

MediaPlayer component with different props.

#### Example

<Canvas of={MediaPlayer.Primary} />

## Usage

```ts
import { MediaPlayer } from "yadl-ui";

const Example = () => {
  return (
      <MediaPlayer />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
