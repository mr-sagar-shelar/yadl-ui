import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PageNotFound from "./PageNotFound.stories.tsx";

<Meta of={PageNotFound} title="unDraw/PageNotFound" />

# PageNotFound

PageNotFound component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={PageNotFound.Primary} />

## Usage

```ts
import { PageNotFound } from "yadl-ui";

const Example = () => {
  return (
      <PageNotFound />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
