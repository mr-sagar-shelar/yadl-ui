import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ProjectCompleted from "./ProjectCompleted.stories.tsx";

<Meta of={ProjectCompleted} title="unDraw/ProjectCompleted" />

# ProjectCompleted

ProjectCompleted component with different props.

#### Example

<Canvas of={ProjectCompleted.Primary} />

## Usage

```ts
import { ProjectCompleted } from "yadl-ui";

const Example = () => {
  return (
      <ProjectCompleted />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
