import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PortfolioFeedback from "./PortfolioFeedback.stories.tsx";

<Meta of={PortfolioFeedback} title="unDraw/PortfolioFeedback" />

# PortfolioFeedback

PortfolioFeedback component with different props.

#### Example

<Canvas of={PortfolioFeedback.Primary} />

## Usage

```ts
import { PortfolioFeedback } from "yadl-ui";

const Example = () => {
  return (
      <PortfolioFeedback />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
