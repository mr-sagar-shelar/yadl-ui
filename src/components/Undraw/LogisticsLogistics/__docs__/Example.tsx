import React, { FC } from "react";
import LogisticsLogistics from "../LogisticsLogistics";

const Example = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100%",
      }}
    >
      <LogisticsLogistics width="500px" height="500px" />
    </div>
  );
};

export default Example;
