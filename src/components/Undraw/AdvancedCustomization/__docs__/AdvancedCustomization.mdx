import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AdvancedCustomization from "./AdvancedCustomization.stories.tsx";

<Meta of={AdvancedCustomization} title="unDraw/AdvancedCustomization" />

# AdvancedCustomization

AdvancedCustomization component with different props.

#### Example

<Canvas of={AdvancedCustomization.Primary} />

## Usage

```ts
import { AdvancedCustomization } from "yadl-ui";

const Example = () => {
  return (
      <AdvancedCustomization />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
