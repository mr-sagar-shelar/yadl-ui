import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ProductIteration from "./ProductIteration.stories.tsx";

<Meta of={ProductIteration} title="unDraw/ProductIteration" />

# ProductIteration

ProductIteration component with different props.

#### Example

<Canvas of={ProductIteration.Primary} />

## Usage

```ts
import { ProductIteration } from "yadl-ui";

const Example = () => {
  return (
      <ProductIteration />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
