import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Server from "./Server.stories.tsx";

<Meta of={Server} title="unDraw/Server" />

# Server

Server component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Server.Primary} />

## Usage

```ts
import { Server } from "yadl-ui";

const Example = () => {
  return (
      <Server />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
