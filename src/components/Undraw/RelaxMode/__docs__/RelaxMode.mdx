import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as RelaxMode from "./RelaxMode.stories.tsx";

<Meta of={RelaxMode} title="unDraw/RelaxMode" />

# RelaxMode

RelaxMode component with different props.

#### Example

<Canvas of={RelaxMode.Primary} />

## Usage

```ts
import { RelaxMode } from "yadl-ui";

const Example = () => {
  return (
      <RelaxMode />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
