import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Server_clusterServerCluster from "./Server_clusterServerCluster.stories.tsx";

<Meta of={Server_clusterServerCluster} title="unDraw/Server_clusterServerCluster" />

# Server_clusterServerCluster

Server_clusterServerCluster component with different props.

#### Example

<Canvas of={Server_clusterServerCluster.Primary} />

## Usage

```ts
import { Server_clusterServerCluster } from "yadl-ui";

const Example = () => {
  return (
      <Server_clusterServerCluster />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
