import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as NavigatorNavigator from "./NavigatorNavigator.stories.tsx";

<Meta of={NavigatorNavigator} title="unDraw/NavigatorNavigator" />

# NavigatorNavigator

NavigatorNavigator component with different props.

#### Example

<Canvas of={NavigatorNavigator.Primary} />

## Usage

```ts
import { NavigatorNavigator } from "yadl-ui";

const Example = () => {
  return (
      <NavigatorNavigator />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
