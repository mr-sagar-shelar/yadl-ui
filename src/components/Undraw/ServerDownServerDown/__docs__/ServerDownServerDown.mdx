import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ServerDownServerDown from "./ServerDownServerDown.stories.tsx";

<Meta of={ServerDownServerDown} title="unDraw/ServerDownServerDown" />

# ServerDownServerDown

ServerDownServerDown component with different props.

#### Example

<Canvas of={ServerDownServerDown.Primary} />

## Usage

```ts
import { ServerDownServerDown } from "yadl-ui";

const Example = () => {
  return (
      <ServerDownServerDown />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
