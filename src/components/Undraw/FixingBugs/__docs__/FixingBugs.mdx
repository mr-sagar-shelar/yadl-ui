import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as FixingBugs from "./FixingBugs.stories.tsx";

<Meta of={FixingBugs} title="unDraw/FixingBugs" />

# FixingBugs

FixingBugs component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={FixingBugs.Primary} />

## Usage

```ts
import { FixingBugs } from "yadl-ui";

const Example = () => {
  return (
      <FixingBugs />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
