import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VibeCoding from "./VibeCoding.stories.tsx";

<Meta of={VibeCoding} title="unDraw/VibeCoding" />

# VibeCoding

VibeCoding component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={VibeCoding.Primary} />

## Usage

```ts
import { VibeCoding } from "yadl-ui";

const Example = () => {
  return (
      <VibeCoding />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
