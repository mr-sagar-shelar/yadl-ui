import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PodcastListener from "./PodcastListener.stories.tsx";

<Meta of={PodcastListener} title="unDraw/PodcastListener" />

# PodcastListener

PodcastListener component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={PodcastListener.Primary} />

## Usage

```ts
import { PodcastListener } from "yadl-ui";

const Example = () => {
  return (
      <PodcastListener />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
