import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Schedule from "./Schedule.stories.tsx";

<Meta of={Schedule} title="unDraw/Schedule" />

# Schedule

Schedule component with different props.

#### Example

<Canvas of={Schedule.Primary} />

## Usage

```ts
import { Schedule } from "yadl-ui";

const Example = () => {
  return (
      <Schedule />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
