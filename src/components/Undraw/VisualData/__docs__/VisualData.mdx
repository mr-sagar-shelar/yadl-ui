import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VisualData from "./VisualData.stories.tsx";

<Meta of={VisualData} title="unDraw/VisualData" />

# VisualData

VisualData component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={VisualData.Primary} />

## Usage

```ts
import { VisualData } from "yadl-ui";

const Example = () => {
  return (
      <VisualData />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
