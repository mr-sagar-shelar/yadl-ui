import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as SmartResize from "./SmartResize.stories.tsx";

<Meta of={SmartResize} title="unDraw/SmartResize" />

# SmartResize

SmartResize component with different props.

#### Example

<Canvas of={SmartResize.Primary} />

## Usage

```ts
import { SmartResize } from "yadl-ui";

const Example = () => {
  return (
      <SmartResize />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
