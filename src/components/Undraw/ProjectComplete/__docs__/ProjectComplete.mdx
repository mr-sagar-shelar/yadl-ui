import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ProjectComplete from "./ProjectComplete.stories.tsx";

<Meta of={ProjectComplete} title="unDraw/ProjectComplete" />

# ProjectComplete

ProjectComplete component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={ProjectComplete.Primary} />

## Usage

```ts
import { ProjectComplete } from "yadl-ui";

const Example = () => {
  return (
      <ProjectComplete />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
