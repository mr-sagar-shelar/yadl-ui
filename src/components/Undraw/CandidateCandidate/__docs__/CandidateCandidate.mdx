import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CandidateCandidate from "./CandidateCandidate.stories.tsx";

<Meta of={CandidateCandidate} title="unDraw/CandidateCandidate" />

# CandidateCandidate

CandidateCandidate component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={CandidateCandidate.Primary} />

## Usage

```ts
import { CandidateCandidate } from "yadl-ui";

const Example = () => {
  return (
      <CandidateCandidate />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
