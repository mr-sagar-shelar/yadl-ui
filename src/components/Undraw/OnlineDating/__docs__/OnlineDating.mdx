import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as OnlineDating from "./OnlineDating.stories.tsx";

<Meta of={OnlineDating} title="unDraw/OnlineDating" />

# OnlineDating

OnlineDating component with different props.

#### Example

<Canvas of={OnlineDating.Primary} />

## Usage

```ts
import { OnlineDating } from "yadl-ui";

const Example = () => {
  return (
      <OnlineDating />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
