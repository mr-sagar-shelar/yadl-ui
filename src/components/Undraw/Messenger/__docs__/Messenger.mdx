import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Messenger from "./Messenger.stories.tsx";

<Meta of={Messenger} title="unDraw/Messenger" />

# Messenger

Messenger component with different props.

#### Example

<Canvas of={Messenger.Primary} />

## Usage

```ts
import { Messenger } from "yadl-ui";

const Example = () => {
  return (
      <Messenger />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
