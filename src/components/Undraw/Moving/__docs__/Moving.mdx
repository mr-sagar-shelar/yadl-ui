import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Moving from "./Moving.stories.tsx";

<Meta of={Moving} title="unDraw/Moving" />

# Moving

Moving component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Moving.Primary} />

## Usage

```ts
import { Moving } from "yadl-ui";

const Example = () => {
  return (
      <Moving />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
