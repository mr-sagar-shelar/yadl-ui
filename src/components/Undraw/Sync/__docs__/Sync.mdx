import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Sync from "./Sync.stories.tsx";

<Meta of={Sync} title="unDraw/Sync" />

# Sync

Sync component with different props.

#### Example

<Canvas of={Sync.Primary} />

## Usage

```ts
import { Sync } from "yadl-ui";

const Example = () => {
  return (
      <Sync />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
