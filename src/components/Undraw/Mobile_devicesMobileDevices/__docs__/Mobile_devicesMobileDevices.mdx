import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Mobile_devicesMobileDevices from "./Mobile_devicesMobileDevices.stories.tsx";

<Meta of={Mobile_devicesMobileDevices} title="unDraw/Mobile_devicesMobileDevices" />

# Mobile_devicesMobileDevices

Mobile_devicesMobileDevices component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Mobile_devicesMobileDevices.Primary} />

## Usage

```ts
import { Mobile_devicesMobileDevices } from "yadl-ui";

const Example = () => {
  return (
      <Mobile_devicesMobileDevices />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
