import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DarkAnalytics from "./DarkAnalytics.stories.tsx";

<Meta of={DarkAnalytics} title="unDraw/DarkAnalytics" />

# DarkAnalytics

DarkAnalytics component with different props.

#### Example

<Canvas of={DarkAnalytics.Primary} />

## Usage

```ts
import { DarkAnalytics } from "yadl-ui";

const Example = () => {
  return (
      <DarkAnalytics />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
