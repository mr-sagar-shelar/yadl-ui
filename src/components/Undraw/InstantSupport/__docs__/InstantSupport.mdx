import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as InstantSupport from "./InstantSupport.stories.tsx";

<Meta of={InstantSupport} title="unDraw/InstantSupport" />

# InstantSupport

InstantSupport component with different props.

#### Example

<Canvas of={InstantSupport.Primary} />

## Usage

```ts
import { InstantSupport } from "yadl-ui";

const Example = () => {
  return (
      <InstantSupport />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
