import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Survey from "./Survey.stories.tsx";

<Meta of={Survey} title="unDraw/Survey" />

# Survey

Survey component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Survey.Primary} />

## Usage

```ts
import { Survey } from "yadl-ui";

const Example = () => {
  return (
      <Survey />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
