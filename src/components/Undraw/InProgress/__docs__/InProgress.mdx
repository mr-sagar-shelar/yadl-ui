import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as InProgress from "./InProgress.stories.tsx";

<Meta of={InProgress} title="unDraw/InProgress" />

# InProgress

InProgress component with different props.

#### Example

<Canvas of={InProgress.Primary} />

## Usage

```ts
import { InProgress } from "yadl-ui";

const Example = () => {
  return (
      <InProgress />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
