import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DesignFeedback from "./DesignFeedback.stories.tsx";

<Meta of={DesignFeedback} title="unDraw/DesignFeedback" />

# DesignFeedback

DesignFeedback component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={DesignFeedback.Primary} />

## Usage

```ts
import { DesignFeedback } from "yadl-ui";

const Example = () => {
  return (
      <DesignFeedback />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
