import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as HackerMindset from "./HackerMindset.stories.tsx";

<Meta of={HackerMindset} title="unDraw/HackerMindset" />

# HackerMindset

HackerMindset component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={HackerMindset.Primary} />

## Usage

```ts
import { HackerMindset } from "yadl-ui";

const Example = () => {
  return (
      <HackerMindset />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
