import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as SharedWorkspace from "./SharedWorkspace.stories.tsx";

<Meta of={SharedWorkspace} title="unDraw/SharedWorkspace" />

# SharedWorkspace

SharedWorkspace component with different props.

#### Example

<Canvas of={SharedWorkspace.Primary} />

## Usage

```ts
import { SharedWorkspace } from "yadl-ui";

const Example = () => {
  return (
      <SharedWorkspace />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
