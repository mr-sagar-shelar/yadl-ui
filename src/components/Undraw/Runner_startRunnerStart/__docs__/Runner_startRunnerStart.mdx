import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Runner_startRunnerStart from "./Runner_startRunnerStart.stories.tsx";

<Meta of={Runner_startRunnerStart} title="unDraw/Runner_startRunnerStart" />

# Runner_startRunnerStart

Runner_startRunnerStart component with different props.

#### Example

<Canvas of={Runner_startRunnerStart.Primary} />

## Usage

```ts
import { Runner_startRunnerStart } from "yadl-ui";

const Example = () => {
  return (
      <Runner_startRunnerStart />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
