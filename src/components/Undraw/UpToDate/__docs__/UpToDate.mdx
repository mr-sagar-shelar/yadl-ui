import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as UpToDate from "./UpToDate.stories.tsx";

<Meta of={UpToDate} title="unDraw/UpToDate" />

# UpToDate

UpToDate component with different props.

#### Example

<Canvas of={UpToDate.Primary} />

## Usage

```ts
import { UpToDate } from "yadl-ui";

const Example = () => {
  return (
      <UpToDate />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
