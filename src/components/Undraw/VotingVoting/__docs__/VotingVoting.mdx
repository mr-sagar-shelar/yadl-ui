import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VotingVoting from "./VotingVoting.stories.tsx";

<Meta of={VotingVoting} title="unDraw/VotingVoting" />

# VotingVoting

VotingVoting component with different props.

#### Example

<Canvas of={VotingVoting.Primary} />

## Usage

```ts
import { VotingVoting } from "yadl-ui";

const Example = () => {
  return (
      <VotingVoting />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
