import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as OnlinePosts from "./OnlinePosts.stories.tsx";

<Meta of={OnlinePosts} title="unDraw/OnlinePosts" />

# OnlinePosts

OnlinePosts component with different props.

#### Example

<Canvas of={OnlinePosts.Primary} />

## Usage

```ts
import { OnlinePosts } from "yadl-ui";

const Example = () => {
  return (
      <OnlinePosts />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
