import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PhotoSession from "./PhotoSession.stories.tsx";

<Meta of={PhotoSession} title="unDraw/PhotoSession" />

# PhotoSession

PhotoSession component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={PhotoSession.Primary} />

## Usage

```ts
import { PhotoSession } from "yadl-ui";

const Example = () => {
  return (
      <PhotoSession />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
