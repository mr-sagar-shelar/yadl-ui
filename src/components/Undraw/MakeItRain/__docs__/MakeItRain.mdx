import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as MakeItRain from "./MakeItRain.stories.tsx";

<Meta of={MakeItRain} title="unDraw/MakeItRain" />

# MakeItRain

MakeItRain component with different props.

#### Example

<Canvas of={MakeItRain.Primary} />

## Usage

```ts
import { MakeItRain } from "yadl-ui";

const Example = () => {
  return (
      <MakeItRain />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
