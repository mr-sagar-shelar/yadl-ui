import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Two_factor_authenticationTwoFactorAuthentication from "./Two_factor_authenticationTwoFactorAuthentication.stories.tsx";

<Meta of={Two_factor_authenticationTwoFactorAuthentication} title="unDraw/Two_factor_authenticationTwoFactorAuthentication" />

# Two_factor_authenticationTwoFactorAuthentication

Two_factor_authenticationTwoFactorAuthentication component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Two_factor_authenticationTwoFactorAuthentication.Primary} />

## Usage

```ts
import { Two_factor_authenticationTwoFactorAuthentication } from "yadl-ui";

const Example = () => {
  return (
      <Two_factor_authenticationTwoFactorAuthentication />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
