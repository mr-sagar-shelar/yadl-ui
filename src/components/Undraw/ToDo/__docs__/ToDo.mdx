import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ToDo from "./ToDo.stories.tsx";

<Meta of={ToDo} title="unDraw/ToDo" />

# ToDo

ToDo component with different props.

#### Example

<Canvas of={ToDo.Primary} />

## Usage

```ts
import { ToDo } from "yadl-ui";

const Example = () => {
  return (
      <ToDo />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
