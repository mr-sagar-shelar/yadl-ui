import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DesignData from "./DesignData.stories.tsx";

<Meta of={DesignData} title="unDraw/DesignData" />

# DesignData

DesignData component with different props.

#### Example

<Canvas of={DesignData.Primary} />

## Usage

```ts
import { DesignData } from "yadl-ui";

const Example = () => {
  return (
      <DesignData />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
