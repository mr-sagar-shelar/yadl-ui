import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as OpenSourceOpenSource from "./OpenSourceOpenSource.stories.tsx";

<Meta of={OpenSourceOpenSource} title="unDraw/OpenSourceOpenSource" />

# OpenSourceOpenSource

OpenSourceOpenSource component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={OpenSourceOpenSource.Primary} />

## Usage

```ts
import { OpenSourceOpenSource } from "yadl-ui";

const Example = () => {
  return (
      <OpenSourceOpenSource />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
