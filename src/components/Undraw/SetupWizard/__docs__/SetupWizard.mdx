import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as SetupWizard from "./SetupWizard.stories.tsx";

<Meta of={SetupWizard} title="unDraw/SetupWizard" />

# SetupWizard

SetupWizard component with different props.

#### Example

<Canvas of={SetupWizard.Primary} />

## Usage

```ts
import { SetupWizard } from "yadl-ui";

const Example = () => {
  return (
      <SetupWizard />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
