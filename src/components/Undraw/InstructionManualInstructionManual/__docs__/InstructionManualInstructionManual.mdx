import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as InstructionManualInstructionManual from "./InstructionManualInstructionManual.stories.tsx";

<Meta of={InstructionManualInstructionManual} title="unDraw/InstructionManualInstructionManual" />

# InstructionManualInstructionManual

InstructionManualInstructionManual component with different props.

#### Example

<Canvas of={InstructionManualInstructionManual.Primary} />

## Usage

```ts
import { InstructionManualInstructionManual } from "yadl-ui";

const Example = () => {
  return (
      <InstructionManualInstructionManual />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
