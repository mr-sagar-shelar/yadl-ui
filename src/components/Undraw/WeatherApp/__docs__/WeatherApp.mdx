import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as WeatherApp from "./WeatherApp.stories.tsx";

<Meta of={WeatherApp} title="unDraw/WeatherApp" />

# WeatherApp

WeatherApp component with different props.

#### Example

<Canvas of={WeatherApp.Primary} />

## Usage

```ts
import { WeatherApp } from "yadl-ui";

const Example = () => {
  return (
      <WeatherApp />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
