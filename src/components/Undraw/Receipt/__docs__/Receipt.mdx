import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Receipt from "./Receipt.stories.tsx";

<Meta of={Receipt} title="unDraw/Receipt" />

# Receipt

Receipt component with different props.

#### Example

<Canvas of={Receipt.Primary} />

## Usage

```ts
import { Receipt } from "yadl-ui";

const Example = () => {
  return (
      <Receipt />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
