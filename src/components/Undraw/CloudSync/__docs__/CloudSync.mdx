import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudSync from "./CloudSync.stories.tsx";

<Meta of={CloudSync} title="unDraw/CloudSync" />

# CloudSync

CloudSync component with different props.

#### Example

<Canvas of={CloudSync.Primary} />

## Usage

```ts
import { CloudSync } from "yadl-ui";

const Example = () => {
  return (
      <CloudSync />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
