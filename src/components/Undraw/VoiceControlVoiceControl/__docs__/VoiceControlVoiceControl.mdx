import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VoiceControlVoiceControl from "./VoiceControlVoiceControl.stories.tsx";

<Meta of={VoiceControlVoiceControl} title="unDraw/VoiceControlVoiceControl" />

# VoiceControlVoiceControl

VoiceControlVoiceControl component with different props.

#### Example

<Canvas of={VoiceControlVoiceControl.Primary} />

## Usage

```ts
import { VoiceControlVoiceControl } from "yadl-ui";

const Example = () => {
  return (
      <VoiceControlVoiceControl />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
