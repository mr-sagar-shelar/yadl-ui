import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as UploadImage from "./UploadImage.stories.tsx";

<Meta of={UploadImage} title="unDraw/UploadImage" />

# UploadImage

UploadImage component with different props.

#### Example

<Canvas of={UploadImage.Primary} />

## Usage

```ts
import { UploadImage } from "yadl-ui";

const Example = () => {
  return (
      <UploadImage />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
