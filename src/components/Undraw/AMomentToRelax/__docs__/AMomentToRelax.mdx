import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AMomentToRelax from "./AMomentToRelax.stories.tsx";

<Meta of={AMomentToRelax} title="unDraw/AMomentToRelax" />

# AMomentToRelax

AMomentToRelax component with different props.

#### Example

<Canvas of={AMomentToRelax.Primary} />

## Usage

```ts
import { AMomentToRelax } from "yadl-ui";

const Example = () => {
  return (
      <AMomentToRelax />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
