import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Loading from "./Loading.stories.tsx";

<Meta of={Loading} title="unDraw/Loading" />

# Loading

Loading component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Loading.Primary} />

## Usage

```ts
import { Loading } from "yadl-ui";

const Example = () => {
  return (
      <Loading />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
