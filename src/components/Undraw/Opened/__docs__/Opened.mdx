import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Opened from "./Opened.stories.tsx";

<Meta of={Opened} title="unDraw/Opened" />

# Opened

Opened component with different props.

#### Example

<Canvas of={Opened.Primary} />

## Usage

```ts
import { Opened } from "yadl-ui";

const Example = () => {
  return (
      <Opened />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
