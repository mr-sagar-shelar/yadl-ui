import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Setup from "./Setup.stories.tsx";

<Meta of={Setup} title="unDraw/Setup" />

# Setup

Setup component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Setup.Primary} />

## Usage

```ts
import { Setup } from "yadl-ui";

const Example = () => {
  return (
      <Setup />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
