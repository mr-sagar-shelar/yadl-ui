import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as FoundingTeam from "./FoundingTeam.stories.tsx";

<Meta of={FoundingTeam} title="unDraw/FoundingTeam" />

# FoundingTeam

FoundingTeam component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={FoundingTeam.Primary} />

## Usage

```ts
import { FoundingTeam } from "yadl-ui";

const Example = () => {
  return (
      <FoundingTeam />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
