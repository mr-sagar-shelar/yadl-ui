import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as LinkLinkShortener from "./LinkLinkShortener.stories.tsx";

<Meta of={LinkLinkShortener} title="unDraw/LinkLinkShortener" />

# LinkLinkShortener

LinkLinkShortener component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={LinkLinkShortener.Primary} />

## Usage

```ts
import { LinkLinkShortener } from "yadl-ui";

const Example = () => {
  return (
      <LinkLinkShortener />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
