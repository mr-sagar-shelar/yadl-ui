import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Convert from "./Convert.stories.tsx";

<Meta of={Convert} title="unDraw/Convert" />

# Convert

Convert component with different props.

#### Example

<Canvas of={Convert.Primary} />

## Usage

```ts
import { Convert } from "yadl-ui";

const Example = () => {
  return (
      <Convert />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
