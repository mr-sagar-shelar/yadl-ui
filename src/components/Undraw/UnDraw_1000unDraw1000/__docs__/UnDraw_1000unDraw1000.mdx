import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as UnDraw_1000unDraw1000 from "./UnDraw_1000unDraw1000.stories.tsx";

<Meta of={UnDraw_1000unDraw1000} title="unDraw/UnDraw_1000unDraw1000" />

# UnDraw_1000unDraw1000

UnDraw_1000unDraw1000 component with different props.

#### Example

<Canvas of={UnDraw_1000unDraw1000.Primary} />

## Usage

```ts
import { UnDraw_1000unDraw1000 } from "yadl-ui";

const Example = () => {
  return (
      <UnDraw_1000unDraw1000 />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
