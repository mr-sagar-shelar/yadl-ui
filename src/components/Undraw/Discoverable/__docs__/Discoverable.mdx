import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Discoverable from "./Discoverable.stories.tsx";

<Meta of={Discoverable} title="unDraw/Discoverable" />

# Discoverable

Discoverable component with different props.

#### Example

<Canvas of={Discoverable.Primary} />

## Usage

```ts
import { Discoverable } from "yadl-ui";

const Example = () => {
  return (
      <Discoverable />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
