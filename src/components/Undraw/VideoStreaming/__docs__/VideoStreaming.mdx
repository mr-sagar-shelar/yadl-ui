import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VideoStreaming from "./VideoStreaming.stories.tsx";

<Meta of={VideoStreaming} title="unDraw/VideoStreaming" />

# VideoStreaming

VideoStreaming component with different props.

#### Example

<Canvas of={VideoStreaming.Primary} />

## Usage

```ts
import { VideoStreaming } from "yadl-ui";

const Example = () => {
  return (
      <VideoStreaming />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
