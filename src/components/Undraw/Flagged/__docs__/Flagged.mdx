import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Flagged from "./Flagged.stories.tsx";

<Meta of={Flagged} title="unDraw/Flagged" />

# Flagged

Flagged component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Flagged.Primary} />

## Usage

```ts
import { Flagged } from "yadl-ui";

const Example = () => {
  return (
      <Flagged />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
