import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as WebDevices from "./WebDevices.stories.tsx";

<Meta of={WebDevices} title="unDraw/WebDevices" />

# WebDevices

WebDevices component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={WebDevices.Primary} />

## Usage

```ts
import { WebDevices } from "yadl-ui";

const Example = () => {
  return (
      <WebDevices />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
