import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as FavoritePost from "./FavoritePost.stories.tsx";

<Meta of={FavoritePost} title="unDraw/FavoritePost" />

# FavoritePost

FavoritePost component with different props.

#### Example

<Canvas of={FavoritePost.Primary} />

## Usage

```ts
import { FavoritePost } from "yadl-ui";

const Example = () => {
  return (
      <FavoritePost />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
