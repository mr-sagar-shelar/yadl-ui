import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PublishArticle from "./PublishArticle.stories.tsx";

<Meta of={PublishArticle} title="unDraw/PublishArticle" />

# PublishArticle

PublishArticle component with different props.

#### Example

<Canvas of={PublishArticle.Primary} />

## Usage

```ts
import { PublishArticle } from "yadl-ui";

const Example = () => {
  return (
      <PublishArticle />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
