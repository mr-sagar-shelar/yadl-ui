import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as WebsiteSetup from "./WebsiteSetup.stories.tsx";

<Meta of={WebsiteSetup} title="unDraw/WebsiteSetup" />

# WebsiteSetup

WebsiteSetup component with different props.

#### Example

<Canvas of={WebsiteSetup.Primary} />

## Usage

```ts
import { WebsiteSetup } from "yadl-ui";

const Example = () => {
  return (
      <WebsiteSetup />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
