import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as UnreadMessages from "./UnreadMessages.stories.tsx";

<Meta of={UnreadMessages} title="unDraw/UnreadMessages" />

# UnreadMessages

UnreadMessages component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={UnreadMessages.Primary} />

## Usage

```ts
import { UnreadMessages } from "yadl-ui";

const Example = () => {
  return (
      <UnreadMessages />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
