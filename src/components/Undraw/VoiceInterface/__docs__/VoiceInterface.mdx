import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VoiceInterface from "./VoiceInterface.stories.tsx";

<Meta of={VoiceInterface} title="unDraw/VoiceInterface" />

# VoiceInterface

VoiceInterface component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={VoiceInterface.Primary} />

## Usage

```ts
import { VoiceInterface } from "yadl-ui";

const Example = () => {
  return (
      <VoiceInterface />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
