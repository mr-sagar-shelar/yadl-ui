import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as MemoryStorageMemoryStorage from "./MemoryStorageMemoryStorage.stories.tsx";

<Meta of={MemoryStorageMemoryStorage} title="unDraw/MemoryStorageMemoryStorage" />

# MemoryStorageMemoryStorage

MemoryStorageMemoryStorage component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={MemoryStorageMemoryStorage.Primary} />

## Usage

```ts
import { MemoryStorageMemoryStorage } from "yadl-ui";

const Example = () => {
  return (
      <MemoryStorageMemoryStorage />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
