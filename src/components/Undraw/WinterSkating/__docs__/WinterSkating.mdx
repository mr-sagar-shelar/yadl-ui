import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as WinterSkating from "./WinterSkating.stories.tsx";

<Meta of={WinterSkating} title="unDraw/WinterSkating" />

# WinterSkating

WinterSkating component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={WinterSkating.Primary} />

## Usage

```ts
import { WinterSkating } from "yadl-ui";

const Example = () => {
  return (
      <WinterSkating />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
