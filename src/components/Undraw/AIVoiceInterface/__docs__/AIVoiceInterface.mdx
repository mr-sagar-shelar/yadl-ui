import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AIVoiceInterface from "./AIVoiceInterface.stories.tsx";

<Meta of={AIVoiceInterface} title="unDraw/AIVoiceInterface" />

# AIVoiceInterface

AIVoiceInterface component with different props.

#### Example

<Canvas of={AIVoiceInterface.Primary} />

## Usage

```ts
import { AIVoiceInterface } from "yadl-ui";

const Example = () => {
  return (
      <AIVoiceInterface />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
