import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Mailbox from "./Mailbox.stories.tsx";

<Meta of={Mailbox} title="unDraw/Mailbox" />

# Mailbox

Mailbox component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Mailbox.Primary} />

## Usage

```ts
import { Mailbox } from "yadl-ui";

const Example = () => {
  return (
      <Mailbox />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
