import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AtWork from "./AtWork.stories.tsx";

<Meta of={AtWork} title="unDraw/AtWork" />

# AtWork

AtWork component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={AtWork.Primary} />

## Usage

```ts
import { AtWork } from "yadl-ui";

const Example = () => {
  return (
      <AtWork />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
