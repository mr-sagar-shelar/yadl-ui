import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DogWalking from "./DogWalking.stories.tsx";

<Meta of={DogWalking} title="unDraw/DogWalking" />

# DogWalking

DogWalking component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={DogWalking.Primary} />

## Usage

```ts
import { DogWalking } from "yadl-ui";

const Example = () => {
  return (
      <DogWalking />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
