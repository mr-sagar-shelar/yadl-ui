import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as StripePayments from "./StripePayments.stories.tsx";

<Meta of={StripePayments} title="unDraw/StripePayments" />

# StripePayments

StripePayments component with different props.

#### Example

<Canvas of={StripePayments.Primary} />

## Usage

```ts
import { StripePayments } from "yadl-ui";

const Example = () => {
  return (
      <StripePayments />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
