import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Calling from "./Calling.stories.tsx";

<Meta of={Calling} title="unDraw/Calling" />

# Calling

Calling component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Calling.Primary} />

## Usage

```ts
import { Calling } from "yadl-ui";

const Example = () => {
  return (
      <Calling />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
