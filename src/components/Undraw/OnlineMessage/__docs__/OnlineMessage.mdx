import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as OnlineMessage from "./OnlineMessage.stories.tsx";

<Meta of={OnlineMessage} title="unDraw/OnlineMessage" />

# OnlineMessage

OnlineMessage component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={OnlineMessage.Primary} />

## Usage

```ts
import { OnlineMessage } from "yadl-ui";

const Example = () => {
  return (
      <OnlineMessage />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
