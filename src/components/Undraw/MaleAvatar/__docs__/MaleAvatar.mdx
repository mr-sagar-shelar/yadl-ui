import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as MaleAvatar from "./MaleAvatar.stories.tsx";

<Meta of={MaleAvatar} title="unDraw/MaleAvatar" />

# MaleAvatar

MaleAvatar component with different props.

#### Example

<Canvas of={MaleAvatar.Primary} />

## Usage

```ts
import { MaleAvatar } from "yadl-ui";

const Example = () => {
  return (
      <MaleAvatar />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
