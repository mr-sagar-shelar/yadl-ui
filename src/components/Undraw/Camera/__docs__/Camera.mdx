import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Camera from "./Camera.stories.tsx";

<Meta of={Camera} title="unDraw/Camera" />

# Camera

Camera component with different props.

#### Example

<Canvas of={Camera.Primary} />

## Usage

```ts
import { Camera } from "yadl-ui";

const Example = () => {
  return (
      <Camera />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
