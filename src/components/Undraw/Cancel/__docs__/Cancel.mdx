import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Cancel from "./Cancel.stories.tsx";

<Meta of={Cancel} title="unDraw/Cancel" />

# Cancel

Cancel component with different props.

#### Example

<Canvas of={Cancel.Primary} />

## Usage

```ts
import { Cancel } from "yadl-ui";

const Example = () => {
  return (
      <Cancel />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
