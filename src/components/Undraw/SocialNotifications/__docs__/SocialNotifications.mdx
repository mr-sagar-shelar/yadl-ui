import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as SocialNotifications from "./SocialNotifications.stories.tsx";

<Meta of={SocialNotifications} title="unDraw/SocialNotifications" />

# SocialNotifications

SocialNotifications component with different props.

#### Example

<Canvas of={SocialNotifications.Primary} />

## Usage

```ts
import { SocialNotifications } from "yadl-ui";

const Example = () => {
  return (
      <SocialNotifications />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
