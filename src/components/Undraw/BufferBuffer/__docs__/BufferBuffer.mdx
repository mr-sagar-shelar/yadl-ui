import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as BufferBuffer from "./BufferBuffer.stories.tsx";

<Meta of={BufferBuffer} title="unDraw/BufferBuffer" />

# BufferBuffer

BufferBuffer component with different props.

#### Example

<Canvas of={BufferBuffer.Primary} />

## Usage

```ts
import { BufferBuffer } from "yadl-ui";

const Example = () => {
  return (
      <BufferBuffer />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
