import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Vault from "./Vault.stories.tsx";

<Meta of={Vault} title="unDraw/Vault" />

# Vault

Vault component with different props.

#### Example

<Canvas of={Vault.Primary} />

## Usage

```ts
import { Vault } from "yadl-ui";

const Example = () => {
  return (
      <Vault />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
