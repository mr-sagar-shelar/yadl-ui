import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as OnlineOrganizer from "./OnlineOrganizer.stories.tsx";

<Meta of={OnlineOrganizer} title="unDraw/OnlineOrganizer" />

# OnlineOrganizer

OnlineOrganizer component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={OnlineOrganizer.Primary} />

## Usage

```ts
import { OnlineOrganizer } from "yadl-ui";

const Example = () => {
  return (
      <OnlineOrganizer />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
