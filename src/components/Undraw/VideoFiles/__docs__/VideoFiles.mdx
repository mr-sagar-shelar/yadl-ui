import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VideoFiles from "./VideoFiles.stories.tsx";

<Meta of={VideoFiles} title="unDraw/VideoFiles" />

# VideoFiles

VideoFiles component with different props.

#### Example

<Canvas of={VideoFiles.Primary} />

## Usage

```ts
import { VideoFiles } from "yadl-ui";

const Example = () => {
  return (
      <VideoFiles />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
