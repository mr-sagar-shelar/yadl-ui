import React, { FC } from "react";
import Next_jsNextJs from "../Next_jsNextJs";

const Example = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100%",
      }}
    >
      <Next_jsNextJs width="500px" height="500px" />
    </div>
  );
};

export default Example;
