import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Next_jsNextJs from "./Next_jsNextJs.stories.tsx";

<Meta of={Next_jsNextJs} title="unDraw/Next_jsNextJs" />

# Next_jsNextJs

Next_jsNextJs component with different props.

#### Example

<Canvas of={Next_jsNextJs.Primary} />

## Usage

```ts
import { Next_jsNextJs } from "yadl-ui";

const Example = () => {
  return (
      <Next_jsNextJs />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
