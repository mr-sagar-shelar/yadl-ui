import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Options from "./Options.stories.tsx";

<Meta of={Options} title="unDraw/Options" />

# Options

Options component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Options.Primary} />

## Usage

```ts
import { Options } from "yadl-ui";

const Example = () => {
  return (
      <Options />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
