import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as WinterWalk from "./WinterWalk.stories.tsx";

<Meta of={WinterWalk} title="unDraw/WinterWalk" />

# WinterWalk

WinterWalk component with different props.

#### Example

<Canvas of={WinterWalk.Primary} />

## Usage

```ts
import { WinterWalk } from "yadl-ui";

const Example = () => {
  return (
      <WinterWalk />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
