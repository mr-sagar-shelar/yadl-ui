import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as SunnyDay from "./SunnyDay.stories.tsx";

<Meta of={SunnyDay} title="unDraw/SunnyDay" />

# SunnyDay

SunnyDay component with different props.

#### Example

<Canvas of={SunnyDay.Primary} />

## Usage

```ts
import { SunnyDay } from "yadl-ui";

const Example = () => {
  return (
      <SunnyDay />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
