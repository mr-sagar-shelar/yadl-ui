import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as OpenNotes from "./OpenNotes.stories.tsx";

<Meta of={OpenNotes} title="unDraw/OpenNotes" />

# OpenNotes

OpenNotes component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={OpenNotes.Primary} />

## Usage

```ts
import { OpenNotes } from "yadl-ui";

const Example = () => {
  return (
      <OpenNotes />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
