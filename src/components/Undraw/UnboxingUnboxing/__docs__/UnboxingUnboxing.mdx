import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as UnboxingUnboxing from "./UnboxingUnboxing.stories.tsx";

<Meta of={UnboxingUnboxing} title="unDraw/UnboxingUnboxing" />

# UnboxingUnboxing

UnboxingUnboxing component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={UnboxingUnboxing.Primary} />

## Usage

```ts
import { UnboxingUnboxing } from "yadl-ui";

const Example = () => {
  return (
      <UnboxingUnboxing />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
