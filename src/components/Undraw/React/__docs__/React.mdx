import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as React from "./React.stories.tsx";

<Meta of={React} title="unDraw/React" />

# React

React component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={React.Primary} />

## Usage

```ts
import { React } from "yadl-ui";

const Example = () => {
  return (
      <React />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
