import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Empty_cartEmptyCart from "./Empty_cartEmptyCart.stories.tsx";

<Meta of={Empty_cartEmptyCart} title="unDraw/Empty_cartEmptyCart" />

# Empty_cartEmptyCart

Empty_cartEmptyCart component with different props.

#### Example

<Canvas of={Empty_cartEmptyCart.Primary} />

## Usage

```ts
import { Empty_cartEmptyCart } from "yadl-ui";

const Example = () => {
  return (
      <Empty_cartEmptyCart />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
