import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DarkMode from "./DarkMode.stories.tsx";

<Meta of={DarkMode} title="unDraw/DarkMode" />

# DarkMode

DarkMode component with different props.

#### Example

<Canvas of={DarkMode.Primary} />

## Usage

```ts
import { DarkMode } from "yadl-ui";

const Example = () => {
  return (
      <DarkMode />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
