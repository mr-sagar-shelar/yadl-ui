import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as FileSync from "./FileSync.stories.tsx";

<Meta of={FileSync} title="unDraw/FileSync" />

# FileSync

FileSync component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={FileSync.Primary} />

## Usage

```ts
import { FileSync } from "yadl-ui";

const Example = () => {
  return (
      <FileSync />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
