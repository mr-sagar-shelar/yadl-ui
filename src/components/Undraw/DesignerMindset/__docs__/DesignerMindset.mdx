import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DesignerMindset from "./DesignerMindset.stories.tsx";

<Meta of={DesignerMindset} title="unDraw/DesignerMindset" />

# DesignerMindset

DesignerMindset component with different props.

#### Example

<Canvas of={DesignerMindset.Primary} />

## Usage

```ts
import { DesignerMindset } from "yadl-ui";

const Example = () => {
  return (
      <DesignerMindset />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
