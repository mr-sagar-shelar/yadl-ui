import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Writer from "./Writer.stories.tsx";

<Meta of={Writer} title="unDraw/Writer" />

# Writer

Writer component with different props.

#### Example

<Canvas of={Writer.Primary} />

## Usage

```ts
import { Writer } from "yadl-ui";

const Example = () => {
  return (
      <Writer />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
