import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as GrowthAnalytics from "./GrowthAnalytics.stories.tsx";

<Meta of={GrowthAnalytics} title="unDraw/GrowthAnalytics" />

# GrowthAnalytics

GrowthAnalytics component with different props.

#### Example

<Canvas of={GrowthAnalytics.Primary} />

## Usage

```ts
import { GrowthAnalytics } from "yadl-ui";

const Example = () => {
  return (
      <GrowthAnalytics />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
