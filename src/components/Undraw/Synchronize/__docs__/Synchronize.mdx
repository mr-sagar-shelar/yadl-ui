import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Synchronize from "./Synchronize.stories.tsx";

<Meta of={Synchronize} title="unDraw/Synchronize" />

# Synchronize

Synchronize component with different props.

#### Example

<Canvas of={Synchronize.Primary} />

## Usage

```ts
import { Synchronize } from "yadl-ui";

const Example = () => {
  return (
      <Synchronize />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
