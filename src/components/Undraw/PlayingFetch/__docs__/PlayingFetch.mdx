import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PlayingFetch from "./PlayingFetch.stories.tsx";

<Meta of={PlayingFetch} title="unDraw/PlayingFetch" />

# PlayingFetch

PlayingFetch component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={PlayingFetch.Primary} />

## Usage

```ts
import { PlayingFetch } from "yadl-ui";

const Example = () => {
  return (
      <PlayingFetch />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
