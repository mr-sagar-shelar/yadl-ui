import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as FileSynchronization from "./FileSynchronization.stories.tsx";

<Meta of={FileSynchronization} title="unDraw/FileSynchronization" />

# FileSynchronization

FileSynchronization component with different props.

#### Example

<Canvas of={FileSynchronization.Primary} />

## Usage

```ts
import { FileSynchronization } from "yadl-ui";

const Example = () => {
  return (
      <FileSynchronization />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
