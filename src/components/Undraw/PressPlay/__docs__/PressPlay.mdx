import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PressPlay from "./PressPlay.stories.tsx";

<Meta of={PressPlay} title="unDraw/PressPlay" />

# PressPlay

PressPlay component with different props.

#### Example

<Canvas of={PressPlay.Primary} />

## Usage

```ts
import { PressPlay } from "yadl-ui";

const Example = () => {
  return (
      <PressPlay />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
