import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CreditCardPayments from "./CreditCardPayments.stories.tsx";

<Meta of={CreditCardPayments} title="unDraw/CreditCardPayments" />

# CreditCardPayments

CreditCardPayments component with different props.

#### Example

<Canvas of={CreditCardPayments.Primary} />

## Usage

```ts
import { CreditCardPayments } from "yadl-ui";

const Example = () => {
  return (
      <CreditCardPayments />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
