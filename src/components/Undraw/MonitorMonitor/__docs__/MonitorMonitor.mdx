import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as MonitorMonitor from "./MonitorMonitor.stories.tsx";

<Meta of={MonitorMonitor} title="unDraw/MonitorMonitor" />

# MonitorMonitor

MonitorMonitor component with different props.

#### Example

<Canvas of={MonitorMonitor.Primary} />

## Usage

```ts
import { MonitorMonitor } from "yadl-ui";

const Example = () => {
  return (
      <MonitorMonitor />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
