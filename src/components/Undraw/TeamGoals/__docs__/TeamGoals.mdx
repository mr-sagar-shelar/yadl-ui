import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as TeamGoals from "./TeamGoals.stories.tsx";

<Meta of={TeamGoals} title="unDraw/TeamGoals" />

# TeamGoals

TeamGoals component with different props.

#### Example

<Canvas of={TeamGoals.Primary} />

## Usage

```ts
import { TeamGoals } from "yadl-ui";

const Example = () => {
  return (
      <TeamGoals />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
