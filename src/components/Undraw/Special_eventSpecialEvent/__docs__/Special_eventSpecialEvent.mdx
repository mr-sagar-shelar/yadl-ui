import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Special_eventSpecialEvent from "./Special_eventSpecialEvent.stories.tsx";

<Meta of={Special_eventSpecialEvent} title="unDraw/Special_eventSpecialEvent" />

# Special_eventSpecialEvent

Special_eventSpecialEvent component with different props.

#### Example

<Canvas of={Special_eventSpecialEvent.Primary} />

## Usage

```ts
import { Special_eventSpecialEvent } from "yadl-ui";

const Example = () => {
  return (
      <Special_eventSpecialEvent />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
