import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ProjectFeedback from "./ProjectFeedback.stories.tsx";

<Meta of={ProjectFeedback} title="unDraw/ProjectFeedback" />

# ProjectFeedback

ProjectFeedback component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={ProjectFeedback.Primary} />

## Usage

```ts
import { ProjectFeedback } from "yadl-ui";

const Example = () => {
  return (
      <ProjectFeedback />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
