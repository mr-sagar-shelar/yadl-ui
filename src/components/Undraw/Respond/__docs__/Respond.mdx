import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Respond from "./Respond.stories.tsx";

<Meta of={Respond} title="unDraw/Respond" />

# Respond

Respond component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Respond.Primary} />

## Usage

```ts
import { Respond } from "yadl-ui";

const Example = () => {
  return (
      <Respond />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
