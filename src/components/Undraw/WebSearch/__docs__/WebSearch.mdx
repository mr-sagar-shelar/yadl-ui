import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as WebSearch from "./WebSearch.stories.tsx";

<Meta of={WebSearch} title="unDraw/WebSearch" />

# WebSearch

WebSearch component with different props.

#### Example

<Canvas of={WebSearch.Primary} />

## Usage

```ts
import { WebSearch } from "yadl-ui";

const Example = () => {
  return (
      <WebSearch />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
