import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ConnectedWorldConnectedWorld from "./ConnectedWorldConnectedWorld.stories.tsx";

<Meta of={ConnectedWorldConnectedWorld} title="unDraw/ConnectedWorldConnectedWorld" />

# ConnectedWorldConnectedWorld

ConnectedWorldConnectedWorld component with different props.

#### Example

<Canvas of={ConnectedWorldConnectedWorld.Primary} />

## Usage

```ts
import { ConnectedWorldConnectedWorld } from "yadl-ui";

const Example = () => {
  return (
      <ConnectedWorldConnectedWorld />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
