import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ServerStatus from "./ServerStatus.stories.tsx";

<Meta of={ServerStatus} title="unDraw/ServerStatus" />

# ServerStatus

ServerStatus component with different props.

#### Example

<Canvas of={ServerStatus.Primary} />

## Usage

```ts
import { ServerStatus } from "yadl-ui";

const Example = () => {
  return (
      <ServerStatus />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
