import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ConfidentialLetter from "./ConfidentialLetter.stories.tsx";

<Meta of={ConfidentialLetter} title="unDraw/ConfidentialLetter" />

# ConfidentialLetter

ConfidentialLetter component with different props.

#### Example

<Canvas of={ConfidentialLetter.Primary} />

## Usage

```ts
import { ConfidentialLetter } from "yadl-ui";

const Example = () => {
  return (
      <ConfidentialLetter />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
