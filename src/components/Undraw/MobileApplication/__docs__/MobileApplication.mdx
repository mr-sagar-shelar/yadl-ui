import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as MobileApplication from "./MobileApplication.stories.tsx";

<Meta of={MobileApplication} title="unDraw/MobileApplication" />

# MobileApplication

MobileApplication component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={MobileApplication.Primary} />

## Usage

```ts
import { MobileApplication } from "yadl-ui";

const Example = () => {
  return (
      <MobileApplication />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
