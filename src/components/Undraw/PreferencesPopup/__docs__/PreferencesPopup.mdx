import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PreferencesPopup from "./PreferencesPopup.stories.tsx";

<Meta of={PreferencesPopup} title="unDraw/PreferencesPopup" />

# PreferencesPopup

PreferencesPopup component with different props.

#### Example

<Canvas of={PreferencesPopup.Primary} />

## Usage

```ts
import { PreferencesPopup } from "yadl-ui";

const Example = () => {
  return (
      <PreferencesPopup />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
