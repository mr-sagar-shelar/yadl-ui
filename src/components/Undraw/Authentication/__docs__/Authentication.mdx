import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Authentication from "./Authentication.stories.tsx";

<Meta of={Authentication} title="unDraw/Authentication" />

# Authentication

Authentication component with different props.
Credit: [unDraw](https://undraw.co/illustrations)

#### Example

<Canvas of={Authentication.Primary} />

## Usage

```ts
import { Authentication } from "yadl-ui";

const Example = () => {
  return (
      <Authentication />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
