import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as BreakingObstaclesBreakingBarriers from "./BreakingObstaclesBreakingBarriers.stories.tsx";

<Meta of={BreakingObstaclesBreakingBarriers} title="unDraw/BreakingObstaclesBreakingBarriers" />

# BreakingObstaclesBreakingBarriers

BreakingObstaclesBreakingBarriers component with different props.

#### Example

<Canvas of={BreakingObstaclesBreakingBarriers.Primary} />

## Usage

```ts
import { BreakingObstaclesBreakingBarriers } from "yadl-ui";

const Example = () => {
  return (
      <BreakingObstaclesBreakingBarriers />
  );
};

export default Example;
```

#### Credits
[unDraw](https://undraw.co/illustrations)
