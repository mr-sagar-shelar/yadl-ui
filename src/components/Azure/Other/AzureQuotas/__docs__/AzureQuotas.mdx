import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AzureQuotas from "./AzureQuotas.stories.tsx";

<Meta of={AzureQuotas} title="Azure/Other/AzureQuotas" />

# AzureQuotas

AzureQuotas component with different props.

#### Example

<Canvas of={AzureQuotas.Primary} />

## Usage

```ts
import { AzureQuotas } from "yadl-ui";

const Example = () => {
  return (
      <AzureQuotas />
  );
};

export default Example;
```

#### Credits
[Azure/Other](https://learn.microsoft.com/en-us/azure/architecture/icons/)
