import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DefenderMeter from "./DefenderMeter.stories.tsx";

<Meta of={DefenderMeter} title="Azure/Other/DefenderMeter" />

# DefenderMeter

DefenderMeter component with different props.

#### Example

<Canvas of={DefenderMeter.Primary} />

## Usage

```ts
import { DefenderMeter } from "yadl-ui";

const Example = () => {
  return (
      <DefenderMeter />
  );
};

export default Example;
```

#### Credits
[Azure/Other](https://learn.microsoft.com/en-us/azure/architecture/icons/)
