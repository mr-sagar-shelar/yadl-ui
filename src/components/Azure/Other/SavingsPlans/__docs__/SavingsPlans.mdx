import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as SavingsPlans from "./SavingsPlans.stories.tsx";

<Meta of={SavingsPlans} title="Azure/Other/SavingsPlans" />

# SavingsPlans

SavingsPlans component with different props.

#### Example

<Canvas of={SavingsPlans.Primary} />

## Usage

```ts
import { SavingsPlans } from "yadl-ui";

const Example = () => {
  return (
      <SavingsPlans />
  );
};

export default Example;
```

#### Credits
[Azure/Other](https://learn.microsoft.com/en-us/azure/architecture/icons/)
