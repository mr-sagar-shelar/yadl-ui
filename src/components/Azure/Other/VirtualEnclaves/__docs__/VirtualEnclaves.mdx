import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VirtualEnclaves from "./VirtualEnclaves.stories.tsx";

<Meta of={VirtualEnclaves} title="Azure/Other/VirtualEnclaves" />

# VirtualEnclaves

VirtualEnclaves component with different props.

#### Example

<Canvas of={VirtualEnclaves.Primary} />

## Usage

```ts
import { VirtualEnclaves } from "yadl-ui";

const Example = () => {
  return (
      <VirtualEnclaves />
  );
};

export default Example;
```

#### Credits
[Azure/Other](https://learn.microsoft.com/en-us/azure/architecture/icons/)
