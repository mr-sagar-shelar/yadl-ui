import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as EndpointAnalytics from "./EndpointAnalytics.stories.tsx";

<Meta of={EndpointAnalytics} title="Azure/Analytics/EndpointAnalytics" />

# EndpointAnalytics

EndpointAnalytics component with different props.

#### Example

<Canvas of={EndpointAnalytics.Primary} />

## Usage

```ts
import { EndpointAnalytics } from "yadl-ui";

const Example = () => {
  return (
      <EndpointAnalytics />
  );
};

export default Example;
```

#### Credits
[Azure/Analytics](https://learn.microsoft.com/en-us/azure/architecture/icons/)
