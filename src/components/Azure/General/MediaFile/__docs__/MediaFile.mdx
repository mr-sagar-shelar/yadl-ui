import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as MediaFile from "./MediaFile.stories.tsx";

<Meta of={MediaFile} title="Azure/General/MediaFile" />

# MediaFile

MediaFile component with different props.

#### Example

<Canvas of={MediaFile.Primary} />

## Usage

```ts
import { MediaFile } from "yadl-ui";

const Example = () => {
  return (
      <MediaFile />
  );
};

export default Example;
```

#### Credits
[Azure/General](https://learn.microsoft.com/en-us/azure/architecture/icons/)
