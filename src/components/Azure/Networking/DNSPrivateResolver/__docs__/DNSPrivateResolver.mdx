import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DNSPrivateResolver from "./DNSPrivateResolver.stories.tsx";

<Meta of={DNSPrivateResolver} title="Azure/Networking/DNSPrivateResolver" />

# DNSPrivateResolver

DNSPrivateResolver component with different props.

#### Example

<Canvas of={DNSPrivateResolver.Primary} />

## Usage

```ts
import { DNSPrivateResolver } from "yadl-ui";

const Example = () => {
  return (
      <DNSPrivateResolver />
  );
};

export default Example;
```

#### Credits
[Azure/Networking](https://learn.microsoft.com/en-us/azure/architecture/icons/)
