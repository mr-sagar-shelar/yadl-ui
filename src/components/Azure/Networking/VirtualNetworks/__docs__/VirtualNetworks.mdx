import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VirtualNetworks from "./VirtualNetworks.stories.tsx";

<Meta of={VirtualNetworks} title="Azure/Networking/VirtualNetworks" />

# VirtualNetworks

VirtualNetworks component with different props.

#### Example

<Canvas of={VirtualNetworks.Primary} />

## Usage

```ts
import { VirtualNetworks } from "yadl-ui";

const Example = () => {
  return (
      <VirtualNetworks />
  );
};

export default Example;
```

#### Credits
[Azure/Networking](https://learn.microsoft.com/en-us/azure/architecture/icons/)
