import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PublicIPAddresses from "./PublicIPAddresses.stories.tsx";

<Meta of={PublicIPAddresses} title="Azure/Networking/PublicIPAddresses" />

# PublicIPAddresses

PublicIPAddresses component with different props.

#### Example

<Canvas of={PublicIPAddresses.Primary} />

## Usage

```ts
import { PublicIPAddresses } from "yadl-ui";

const Example = () => {
  return (
      <PublicIPAddresses />
  );
};

export default Example;
```

#### Credits
[Azure/Networking](https://learn.microsoft.com/en-us/azure/architecture/icons/)
