import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ServiceEndpointPolicies from "./ServiceEndpointPolicies.stories.tsx";

<Meta of={ServiceEndpointPolicies} title="Azure/Networking/ServiceEndpointPolicies" />

# ServiceEndpointPolicies

ServiceEndpointPolicies component with different props.

#### Example

<Canvas of={ServiceEndpointPolicies.Primary} />

## Usage

```ts
import { ServiceEndpointPolicies } from "yadl-ui";

const Example = () => {
  return (
      <ServiceEndpointPolicies />
  );
};

export default Example;
```

#### Credits
[Azure/Networking](https://learn.microsoft.com/en-us/azure/architecture/icons/)
