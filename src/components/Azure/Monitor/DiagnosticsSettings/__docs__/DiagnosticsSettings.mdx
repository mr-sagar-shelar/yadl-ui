import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DiagnosticsSettings from "./DiagnosticsSettings.stories.tsx";

<Meta of={DiagnosticsSettings} title="Azure/Monitor/DiagnosticsSettings" />

# DiagnosticsSettings

DiagnosticsSettings component with different props.

#### Example

<Canvas of={DiagnosticsSettings.Primary} />

## Usage

```ts
import { DiagnosticsSettings } from "yadl-ui";

const Example = () => {
  return (
      <DiagnosticsSettings />
  );
};

export default Example;
```

#### Credits
[Azure/Monitor](https://learn.microsoft.com/en-us/azure/architecture/icons/)
