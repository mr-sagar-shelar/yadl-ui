import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DeviceConfiguration from "./DeviceConfiguration.stories.tsx";

<Meta of={DeviceConfiguration} title="Azure/Intune/DeviceConfiguration" />

# DeviceConfiguration

DeviceConfiguration component with different props.

#### Example

<Canvas of={DeviceConfiguration.Primary} />

## Usage

```ts
import { DeviceConfiguration } from "yadl-ui";

const Example = () => {
  return (
      <DeviceConfiguration />
  );
};

export default Example;
```

#### Credits
[Azure/Intune](https://learn.microsoft.com/en-us/azure/architecture/icons/)
