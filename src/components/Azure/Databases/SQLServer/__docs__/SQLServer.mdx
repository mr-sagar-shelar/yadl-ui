import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as SQLServer from "./SQLServer.stories.tsx";

<Meta of={SQLServer} title="Azure/Databases/SQLServer" />

# SQLServer

SQLServer component with different props.

#### Example

<Canvas of={SQLServer.Primary} />

## Usage

```ts
import { SQLServer } from "yadl-ui";

const Example = () => {
  return (
      <SQLServer />
  );
};

export default Example;
```

#### Credits
[Azure/Databases](https://learn.microsoft.com/en-us/azure/architecture/icons/)
