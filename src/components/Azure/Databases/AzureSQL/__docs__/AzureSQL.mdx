import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AzureSQL from "./AzureSQL.stories.tsx";

<Meta of={AzureSQL} title="Azure/Databases/AzureSQL" />

# AzureSQL

AzureSQL component with different props.

#### Example

<Canvas of={AzureSQL.Primary} />

## Usage

```ts
import { AzureSQL } from "yadl-ui";

const Example = () => {
  return (
      <AzureSQL />
  );
};

export default Example;
```

#### Credits
[Azure/Databases](https://learn.microsoft.com/en-us/azure/architecture/icons/)
