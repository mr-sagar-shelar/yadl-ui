import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VirtualMachine from "./VirtualMachine.stories.tsx";

<Meta of={VirtualMachine} title="Azure/Compute/VirtualMachine" />

# VirtualMachine

VirtualMachine component with different props.

#### Example

<Canvas of={VirtualMachine.Primary} />

## Usage

```ts
import { VirtualMachine } from "yadl-ui";

const Example = () => {
  return (
      <VirtualMachine />
  );
};

export default Example;
```

#### Credits
[Azure/Compute](https://learn.microsoft.com/en-us/azure/architecture/icons/)
