import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ContainerInstances from "./ContainerInstances.stories.tsx";

<Meta of={ContainerInstances} title="Azure/Compute/ContainerInstances" />

# ContainerInstances

ContainerInstances component with different props.

#### Example

<Canvas of={ContainerInstances.Primary} />

## Usage

```ts
import { ContainerInstances } from "yadl-ui";

const Example = () => {
  return (
      <ContainerInstances />
  );
};

export default Example;
```

#### Credits
[Azure/Compute](https://learn.microsoft.com/en-us/azure/architecture/icons/)
