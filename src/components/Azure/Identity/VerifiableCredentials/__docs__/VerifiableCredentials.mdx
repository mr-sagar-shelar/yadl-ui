import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VerifiableCredentials from "./VerifiableCredentials.stories.tsx";

<Meta of={VerifiableCredentials} title="Azure/Identity/VerifiableCredentials" />

# VerifiableCredentials

VerifiableCredentials component with different props.

#### Example

<Canvas of={VerifiableCredentials.Primary} />

## Usage

```ts
import { VerifiableCredentials } from "yadl-ui";

const Example = () => {
  return (
      <VerifiableCredentials />
  );
};

export default Example;
```

#### Credits
[Azure/Identity](https://learn.microsoft.com/en-us/azure/architecture/icons/)
