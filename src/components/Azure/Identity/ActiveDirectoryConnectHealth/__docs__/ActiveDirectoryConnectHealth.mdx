import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ActiveDirectoryConnectHealth from "./ActiveDirectoryConnectHealth.stories.tsx";

<Meta of={ActiveDirectoryConnectHealth} title="Azure/Identity/ActiveDirectoryConnectHealth" />

# ActiveDirectoryConnectHealth

ActiveDirectoryConnectHealth component with different props.

#### Example

<Canvas of={ActiveDirectoryConnectHealth.Primary} />

## Usage

```ts
import { ActiveDirectoryConnectHealth } from "yadl-ui";

const Example = () => {
  return (
      <ActiveDirectoryConnectHealth />
  );
};

export default Example;
```

#### Credits
[Azure/Identity](https://learn.microsoft.com/en-us/azure/architecture/icons/)
