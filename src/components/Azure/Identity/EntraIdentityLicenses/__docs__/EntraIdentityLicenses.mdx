import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as EntraIdentityLicenses from "./EntraIdentityLicenses.stories.tsx";

<Meta of={EntraIdentityLicenses} title="Azure/Identity/EntraIdentityLicenses" />

# EntraIdentityLicenses

EntraIdentityLicenses component with different props.

#### Example

<Canvas of={EntraIdentityLicenses.Primary} />

## Usage

```ts
import { EntraIdentityLicenses } from "yadl-ui";

const Example = () => {
  return (
      <EntraIdentityLicenses />
  );
};

export default Example;
```

#### Credits
[Azure/Identity](https://learn.microsoft.com/en-us/azure/architecture/icons/)
