import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as EntraConnectSync from "./EntraConnectSync.stories.tsx";

<Meta of={EntraConnectSync} title="Azure/Identity/EntraConnectSync" />

# EntraConnectSync

EntraConnectSync component with different props.

#### Example

<Canvas of={EntraConnectSync.Primary} />

## Usage

```ts
import { EntraConnectSync } from "yadl-ui";

const Example = () => {
  return (
      <EntraConnectSync />
  );
};

export default Example;
```

#### Credits
[Azure/Identity](https://learn.microsoft.com/en-us/azure/architecture/icons/)
