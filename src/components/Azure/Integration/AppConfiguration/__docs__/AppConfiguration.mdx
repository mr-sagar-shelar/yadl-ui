import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AppConfiguration from "./AppConfiguration.stories.tsx";

<Meta of={AppConfiguration} title="Azure/Integration/AppConfiguration" />

# AppConfiguration

AppConfiguration component with different props.

#### Example

<Canvas of={AppConfiguration.Primary} />

## Usage

```ts
import { AppConfiguration } from "yadl-ui";

const Example = () => {
  return (
      <AppConfiguration />
  );
};

export default Example;
```

#### Credits
[Azure/Integration](https://learn.microsoft.com/en-us/azure/architecture/icons/)
