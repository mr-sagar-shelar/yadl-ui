import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Relays from "./Relays.stories.tsx";

<Meta of={Relays} title="Azure/Integration/Relays" />

# Relays

Relays component with different props.

#### Example

<Canvas of={Relays.Primary} />

## Usage

```ts
import { Relays } from "yadl-ui";

const Example = () => {
  return (
      <Relays />
  );
};

export default Example;
```

#### Credits
[Azure/Integration](https://learn.microsoft.com/en-us/azure/architecture/icons/)
