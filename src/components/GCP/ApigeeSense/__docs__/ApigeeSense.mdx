import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ApigeeSense from "./ApigeeSense.stories.tsx";

<Meta of={ApigeeSense} title="GCP/ApigeeSense" />

# ApigeeSense

ApigeeSense component with different props.

#### Example

<Canvas of={ApigeeSense.Primary} />

## Usage

```ts
import { ApigeeSense } from "yadl-ui";

const Example = () => {
  return (
      <ApigeeSense />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
