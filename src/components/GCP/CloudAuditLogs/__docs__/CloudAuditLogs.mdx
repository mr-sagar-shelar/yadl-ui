import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudAuditLogs from "./CloudAuditLogs.stories.tsx";

<Meta of={CloudAuditLogs} title="GCP/CloudAuditLogs" />

# CloudAuditLogs

CloudAuditLogs component with different props.

#### Example

<Canvas of={CloudAuditLogs.Primary} />

## Usage

```ts
import { CloudAuditLogs } from "yadl-ui";

const Example = () => {
  return (
      <CloudAuditLogs />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
