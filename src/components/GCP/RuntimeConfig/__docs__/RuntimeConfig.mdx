import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as RuntimeConfig from "./RuntimeConfig.stories.tsx";

<Meta of={RuntimeConfig} title="GCP/RuntimeConfig" />

# RuntimeConfig

RuntimeConfig component with different props.

#### Example

<Canvas of={RuntimeConfig.Primary} />

## Usage

```ts
import { RuntimeConfig } from "yadl-ui";

const Example = () => {
  return (
      <RuntimeConfig />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
