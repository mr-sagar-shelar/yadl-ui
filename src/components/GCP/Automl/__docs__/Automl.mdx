import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Automl from "./Automl.stories.tsx";

<Meta of={Automl} title="GCP/Automl" />

# Automl

Automl component with different props.

#### Example

<Canvas of={Automl.Primary} />

## Usage

```ts
import { Automl } from "yadl-ui";

const Example = () => {
  return (
      <Automl />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
