import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudForMarketing from "./CloudForMarketing.stories.tsx";

<Meta of={CloudForMarketing} title="GCP/CloudForMarketing" />

# CloudForMarketing

CloudForMarketing component with different props.

#### Example

<Canvas of={CloudForMarketing.Primary} />

## Usage

```ts
import { CloudForMarketing } from "yadl-ui";

const Example = () => {
  return (
      <CloudForMarketing />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
