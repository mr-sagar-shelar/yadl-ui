import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PartnerInterconnect from "./PartnerInterconnect.stories.tsx";

<Meta of={PartnerInterconnect} title="GCP/PartnerInterconnect" />

# PartnerInterconnect

PartnerInterconnect component with different props.

#### Example

<Canvas of={PartnerInterconnect.Primary} />

## Usage

```ts
import { PartnerInterconnect } from "yadl-ui";

const Example = () => {
  return (
      <PartnerInterconnect />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
