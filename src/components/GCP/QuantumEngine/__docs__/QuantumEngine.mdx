import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as QuantumEngine from "./QuantumEngine.stories.tsx";

<Meta of={QuantumEngine} title="GCP/QuantumEngine" />

# QuantumEngine

QuantumEngine component with different props.

#### Example

<Canvas of={QuantumEngine.Primary} />

## Usage

```ts
import { QuantumEngine } from "yadl-ui";

const Example = () => {
  return (
      <QuantumEngine />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
