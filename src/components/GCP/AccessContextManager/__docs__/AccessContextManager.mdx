import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AccessContextManager from "./AccessContextManager.stories.tsx";

<Meta of={AccessContextManager} title="GCP/AccessContextManager" />

# AccessContextManager

AccessContextManager component with different props.

#### Example

<Canvas of={AccessContextManager.Primary} />

## Usage

```ts
import { AccessContextManager } from "yadl-ui";

const Example = () => {
  return (
      <AccessContextManager />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
