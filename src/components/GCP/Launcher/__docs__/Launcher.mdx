import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Launcher from "./Launcher.stories.tsx";

<Meta of={Launcher} title="GCP/Launcher" />

# Launcher

Launcher component with different props.

#### Example

<Canvas of={Launcher.Primary} />

## Usage

```ts
import { Launcher } from "yadl-ui";

const Example = () => {
  return (
      <Launcher />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
