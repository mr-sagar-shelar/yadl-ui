import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as NetworkConnectivityCenter from "./NetworkConnectivityCenter.stories.tsx";

<Meta of={NetworkConnectivityCenter} title="GCP/NetworkConnectivityCenter" />

# NetworkConnectivityCenter

NetworkConnectivityCenter component with different props.

#### Example

<Canvas of={NetworkConnectivityCenter.Primary} />

## Usage

```ts
import { NetworkConnectivityCenter } from "yadl-ui";

const Example = () => {
  return (
      <NetworkConnectivityCenter />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
