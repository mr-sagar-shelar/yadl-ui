import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DataLayers from "./DataLayers.stories.tsx";

<Meta of={DataLayers} title="GCP/DataLayers" />

# DataLayers

DataLayers component with different props.

#### Example

<Canvas of={DataLayers.Primary} />

## Usage

```ts
import { DataLayers } from "yadl-ui";

const Example = () => {
  return (
      <DataLayers />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
