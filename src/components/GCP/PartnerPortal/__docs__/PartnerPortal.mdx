import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PartnerPortal from "./PartnerPortal.stories.tsx";

<Meta of={PartnerPortal} title="GCP/PartnerPortal" />

# PartnerPortal

PartnerPortal component with different props.

#### Example

<Canvas of={PartnerPortal.Primary} />

## Usage

```ts
import { PartnerPortal } from "yadl-ui";

const Example = () => {
  return (
      <PartnerPortal />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
