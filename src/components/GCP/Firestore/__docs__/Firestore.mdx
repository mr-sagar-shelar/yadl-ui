import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Firestore from "./Firestore.stories.tsx";

<Meta of={Firestore} title="GCP/Firestore" />

# Firestore

Firestore component with different props.

#### Example

<Canvas of={Firestore.Primary} />

## Usage

```ts
import { Firestore } from "yadl-ui";

const Example = () => {
  return (
      <Firestore />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
