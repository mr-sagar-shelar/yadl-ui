import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudTpu from "./CloudTpu.stories.tsx";

<Meta of={CloudTpu} title="GCP/CloudTpu" />

# CloudTpu

CloudTpu component with different props.

#### Example

<Canvas of={CloudTpu.Primary} />

## Usage

```ts
import { CloudTpu } from "yadl-ui";

const Example = () => {
  return (
      <CloudTpu />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
