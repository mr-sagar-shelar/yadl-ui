import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Genomics from "./Genomics.stories.tsx";

<Meta of={Genomics} title="GCP/Genomics" />

# Genomics

Genomics component with different props.

#### Example

<Canvas of={Genomics.Primary} />

## Usage

```ts
import { Genomics } from "yadl-ui";

const Example = () => {
  return (
      <Genomics />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
