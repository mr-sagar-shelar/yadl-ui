import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as FleetEngine from "./FleetEngine.stories.tsx";

<Meta of={FleetEngine} title="GCP/FleetEngine" />

# FleetEngine

FleetEngine component with different props.

#### Example

<Canvas of={FleetEngine.Primary} />

## Usage

```ts
import { FleetEngine } from "yadl-ui";

const Example = () => {
  return (
      <FleetEngine />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
