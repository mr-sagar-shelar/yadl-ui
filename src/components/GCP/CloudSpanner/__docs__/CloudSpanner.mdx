import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudSpanner from "./CloudSpanner.stories.tsx";

<Meta of={CloudSpanner} title="GCP/CloudSpanner" />

# CloudSpanner

CloudSpanner component with different props.

#### Example

<Canvas of={CloudSpanner.Primary} />

## Usage

```ts
import { CloudSpanner } from "yadl-ui";

const Example = () => {
  return (
      <CloudSpanner />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
