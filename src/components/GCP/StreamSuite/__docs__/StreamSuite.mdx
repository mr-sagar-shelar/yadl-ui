import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as StreamSuite from "./StreamSuite.stories.tsx";

<Meta of={StreamSuite} title="GCP/StreamSuite" />

# StreamSuite

StreamSuite component with different props.

#### Example

<Canvas of={StreamSuite.Primary} />

## Usage

```ts
import { StreamSuite } from "yadl-ui";

const Example = () => {
  return (
      <StreamSuite />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
