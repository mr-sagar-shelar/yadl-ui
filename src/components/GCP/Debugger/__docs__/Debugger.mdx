import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Debugger from "./Debugger.stories.tsx";

<Meta of={Debugger} title="GCP/Debugger" />

# Debugger

Debugger component with different props.

#### Example

<Canvas of={Debugger.Primary} />

## Usage

```ts
import { Debugger } from "yadl-ui";

const Example = () => {
  return (
      <Debugger />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
