import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as GoogleCloudMarketplace from "./GoogleCloudMarketplace.stories.tsx";

<Meta of={GoogleCloudMarketplace} title="GCP/GoogleCloudMarketplace" />

# GoogleCloudMarketplace

GoogleCloudMarketplace component with different props.

#### Example

<Canvas of={GoogleCloudMarketplace.Primary} />

## Usage

```ts
import { GoogleCloudMarketplace } from "yadl-ui";

const Example = () => {
  return (
      <GoogleCloudMarketplace />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
