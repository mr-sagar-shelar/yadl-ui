import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudBuild from "./CloudBuild.stories.tsx";

<Meta of={CloudBuild} title="GCP/CloudBuild" />

# CloudBuild

CloudBuild component with different props.

#### Example

<Canvas of={CloudBuild.Primary} />

## Usage

```ts
import { CloudBuild } from "yadl-ui";

const Example = () => {
  return (
      <CloudBuild />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
