import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ArtifactRegistry from "./ArtifactRegistry.stories.tsx";

<Meta of={ArtifactRegistry} title="GCP/ArtifactRegistry" />

# ArtifactRegistry

ArtifactRegistry component with different props.

#### Example

<Canvas of={ArtifactRegistry.Primary} />

## Usage

```ts
import { ArtifactRegistry } from "yadl-ui";

const Example = () => {
  return (
      <ArtifactRegistry />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
