import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Dataflow from "./Dataflow.stories.tsx";

<Meta of={Dataflow} title="GCP/Dataflow" />

# Dataflow

Dataflow component with different props.

#### Example

<Canvas of={Dataflow.Primary} />

## Usage

```ts
import { Dataflow } from "yadl-ui";

const Example = () => {
  return (
      <Dataflow />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
