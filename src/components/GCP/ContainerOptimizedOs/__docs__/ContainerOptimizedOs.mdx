import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ContainerOptimizedOs from "./ContainerOptimizedOs.stories.tsx";

<Meta of={ContainerOptimizedOs} title="GCP/ContainerOptimizedOs" />

# ContainerOptimizedOs

ContainerOptimizedOs component with different props.

#### Example

<Canvas of={ContainerOptimizedOs.Primary} />

## Usage

```ts
import { ContainerOptimizedOs } from "yadl-ui";

const Example = () => {
  return (
      <ContainerOptimizedOs />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
