import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Quotas from "./Quotas.stories.tsx";

<Meta of={Quotas} title="GCP/Quotas" />

# Quotas

Quotas component with different props.

#### Example

<Canvas of={Quotas.Primary} />

## Usage

```ts
import { Quotas } from "yadl-ui";

const Example = () => {
  return (
      <Quotas />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
