import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudIds from "./CloudIds.stories.tsx";

<Meta of={CloudIds} title="GCP/CloudIds" />

# CloudIds

CloudIds component with different props.

#### Example

<Canvas of={CloudIds.Primary} />

## Usage

```ts
import { CloudIds } from "yadl-ui";

const Example = () => {
  return (
      <CloudIds />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
