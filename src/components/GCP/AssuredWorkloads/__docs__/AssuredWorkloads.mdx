import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AssuredWorkloads from "./AssuredWorkloads.stories.tsx";

<Meta of={AssuredWorkloads} title="GCP/AssuredWorkloads" />

# AssuredWorkloads

AssuredWorkloads component with different props.

#### Example

<Canvas of={AssuredWorkloads.Primary} />

## Usage

```ts
import { AssuredWorkloads } from "yadl-ui";

const Example = () => {
  return (
      <AssuredWorkloads />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
