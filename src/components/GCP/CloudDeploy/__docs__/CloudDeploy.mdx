import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudDeploy from "./CloudDeploy.stories.tsx";

<Meta of={CloudDeploy} title="GCP/CloudDeploy" />

# CloudDeploy

CloudDeploy component with different props.

#### Example

<Canvas of={CloudDeploy.Primary} />

## Usage

```ts
import { CloudDeploy } from "yadl-ui";

const Example = () => {
  return (
      <CloudDeploy />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
