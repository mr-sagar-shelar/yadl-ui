import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Profiler from "./Profiler.stories.tsx";

<Meta of={Profiler} title="GCP/Profiler" />

# Profiler

Profiler component with different props.

#### Example

<Canvas of={Profiler.Primary} />

## Usage

```ts
import { Profiler } from "yadl-ui";

const Example = () => {
  return (
      <Profiler />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
