import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as IotEdge from "./IotEdge.stories.tsx";

<Meta of={IotEdge} title="GCP/IotEdge" />

# IotEdge

IotEdge component with different props.

#### Example

<Canvas of={IotEdge.Primary} />

## Usage

```ts
import { IotEdge } from "yadl-ui";

const Example = () => {
  return (
      <IotEdge />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
