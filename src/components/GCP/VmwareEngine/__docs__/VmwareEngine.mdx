import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VmwareEngine from "./VmwareEngine.stories.tsx";

<Meta of={VmwareEngine} title="GCP/VmwareEngine" />

# VmwareEngine

VmwareEngine component with different props.

#### Example

<Canvas of={VmwareEngine.Primary} />

## Usage

```ts
import { VmwareEngine } from "yadl-ui";

const Example = () => {
  return (
      <VmwareEngine />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
