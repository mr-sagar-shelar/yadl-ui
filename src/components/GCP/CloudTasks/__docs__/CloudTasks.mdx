import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudTasks from "./CloudTasks.stories.tsx";

<Meta of={CloudTasks} title="GCP/CloudTasks" />

# CloudTasks

CloudTasks component with different props.

#### Example

<Canvas of={CloudTasks.Primary} />

## Usage

```ts
import { CloudTasks } from "yadl-ui";

const Example = () => {
  return (
      <CloudTasks />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
