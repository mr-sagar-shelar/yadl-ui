import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ProducerPortal from "./ProducerPortal.stories.tsx";

<Meta of={ProducerPortal} title="GCP/ProducerPortal" />

# ProducerPortal

ProducerPortal component with different props.

#### Example

<Canvas of={ProducerPortal.Primary} />

## Usage

```ts
import { ProducerPortal } from "yadl-ui";

const Example = () => {
  return (
      <ProducerPortal />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
