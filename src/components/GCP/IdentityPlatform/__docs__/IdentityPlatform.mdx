import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as IdentityPlatform from "./IdentityPlatform.stories.tsx";

<Meta of={IdentityPlatform} title="GCP/IdentityPlatform" />

# IdentityPlatform

IdentityPlatform component with different props.

#### Example

<Canvas of={IdentityPlatform.Primary} />

## Usage

```ts
import { IdentityPlatform } from "yadl-ui";

const Example = () => {
  return (
      <IdentityPlatform />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
