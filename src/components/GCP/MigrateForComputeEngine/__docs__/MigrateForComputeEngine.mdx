import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as MigrateForComputeEngine from "./MigrateForComputeEngine.stories.tsx";

<Meta of={MigrateForComputeEngine} title="GCP/MigrateForComputeEngine" />

# MigrateForComputeEngine

MigrateForComputeEngine component with different props.

#### Example

<Canvas of={MigrateForComputeEngine.Primary} />

## Usage

```ts
import { MigrateForComputeEngine } from "yadl-ui";

const Example = () => {
  return (
      <MigrateForComputeEngine />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
