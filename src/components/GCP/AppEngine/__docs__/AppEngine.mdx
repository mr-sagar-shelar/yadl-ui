import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AppEngine from "./AppEngine.stories.tsx";

<Meta of={AppEngine} title="GCP/AppEngine" />

# AppEngine

AppEngine component with different props.

#### Example

<Canvas of={AppEngine.Primary} />

## Usage

```ts
import { AppEngine } from "yadl-ui";

const Example = () => {
  return (
      <AppEngine />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
