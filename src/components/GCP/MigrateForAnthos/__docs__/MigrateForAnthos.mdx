import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as MigrateForAnthos from "./MigrateForAnthos.stories.tsx";

<Meta of={MigrateForAnthos} title="GCP/MigrateForAnthos" />

# MigrateForAnthos

MigrateForAnthos component with different props.

#### Example

<Canvas of={MigrateForAnthos.Primary} />

## Usage

```ts
import { MigrateForAnthos } from "yadl-ui";

const Example = () => {
  return (
      <MigrateForAnthos />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
