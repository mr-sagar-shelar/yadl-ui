import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as EarlyAccessCenter from "./EarlyAccessCenter.stories.tsx";

<Meta of={EarlyAccessCenter} title="GCP/EarlyAccessCenter" />

# EarlyAccessCenter

EarlyAccessCenter component with different props.

#### Example

<Canvas of={EarlyAccessCenter.Primary} />

## Usage

```ts
import { EarlyAccessCenter } from "yadl-ui";

const Example = () => {
  return (
      <EarlyAccessCenter />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
