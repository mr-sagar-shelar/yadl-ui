import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudDeploymentManager from "./CloudDeploymentManager.stories.tsx";

<Meta of={CloudDeploymentManager} title="GCP/CloudDeploymentManager" />

# CloudDeploymentManager

CloudDeploymentManager component with different props.

#### Example

<Canvas of={CloudDeploymentManager.Primary} />

## Usage

```ts
import { CloudDeploymentManager } from "yadl-ui";

const Example = () => {
  return (
      <CloudDeploymentManager />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
