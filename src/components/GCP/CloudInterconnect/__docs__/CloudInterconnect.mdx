import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudInterconnect from "./CloudInterconnect.stories.tsx";

<Meta of={CloudInterconnect} title="GCP/CloudInterconnect" />

# CloudInterconnect

CloudInterconnect component with different props.

#### Example

<Canvas of={CloudInterconnect.Primary} />

## Usage

```ts
import { CloudInterconnect } from "yadl-ui";

const Example = () => {
  return (
      <CloudInterconnect />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
