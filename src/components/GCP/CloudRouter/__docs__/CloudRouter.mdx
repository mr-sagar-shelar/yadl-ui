import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudRouter from "./CloudRouter.stories.tsx";

<Meta of={CloudRouter} title="GCP/CloudRouter" />

# CloudRouter

CloudRouter component with different props.

#### Example

<Canvas of={CloudRouter.Primary} />

## Usage

```ts
import { CloudRouter } from "yadl-ui";

const Example = () => {
  return (
      <CloudRouter />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
