import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Pubsub from "./Pubsub.stories.tsx";

<Meta of={Pubsub} title="GCP/Pubsub" />

# Pubsub

Pubsub component with different props.

#### Example

<Canvas of={Pubsub.Primary} />

## Usage

```ts
import { Pubsub } from "yadl-ui";

const Example = () => {
  return (
      <Pubsub />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
