import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudNetwork from "./CloudNetwork.stories.tsx";

<Meta of={CloudNetwork} title="GCP/CloudNetwork" />

# CloudNetwork

CloudNetwork component with different props.

#### Example

<Canvas of={CloudNetwork.Primary} />

## Usage

```ts
import { CloudNetwork } from "yadl-ui";

const Example = () => {
  return (
      <CloudNetwork />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
