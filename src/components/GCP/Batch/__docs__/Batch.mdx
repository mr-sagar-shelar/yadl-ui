import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Batch from "./Batch.stories.tsx";

<Meta of={Batch} title="GCP/Batch" />

# Batch

Batch component with different props.

#### Example

<Canvas of={Batch.Primary} />

## Usage

```ts
import { Batch } from "yadl-ui";

const Example = () => {
  return (
      <Batch />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
