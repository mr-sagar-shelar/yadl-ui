import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudInferenceApi from "./CloudInferenceApi.stories.tsx";

<Meta of={CloudInferenceApi} title="GCP/CloudInferenceApi" />

# CloudInferenceApi

CloudInferenceApi component with different props.

#### Example

<Canvas of={CloudInferenceApi.Primary} />

## Usage

```ts
import { CloudInferenceApi } from "yadl-ui";

const Example = () => {
  return (
      <CloudInferenceApi />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
