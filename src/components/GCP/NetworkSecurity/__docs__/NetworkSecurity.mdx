import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as NetworkSecurity from "./NetworkSecurity.stories.tsx";

<Meta of={NetworkSecurity} title="GCP/NetworkSecurity" />

# NetworkSecurity

NetworkSecurity component with different props.

#### Example

<Canvas of={NetworkSecurity.Primary} />

## Usage

```ts
import { NetworkSecurity } from "yadl-ui";

const Example = () => {
  return (
      <NetworkSecurity />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
