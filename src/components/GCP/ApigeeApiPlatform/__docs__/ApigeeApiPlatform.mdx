import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ApigeeApiPlatform from "./ApigeeApiPlatform.stories.tsx";

<Meta of={ApigeeApiPlatform} title="GCP/ApigeeApiPlatform" />

# ApigeeApiPlatform

ApigeeApiPlatform component with different props.

#### Example

<Canvas of={ApigeeApiPlatform.Primary} />

## Usage

```ts
import { ApigeeApiPlatform } from "yadl-ui";

const Example = () => {
  return (
      <ApigeeApiPlatform />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
