import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DataQna from "./DataQna.stories.tsx";

<Meta of={DataQna} title="GCP/DataQna" />

# DataQna

DataQna component with different props.

#### Example

<Canvas of={DataQna.Primary} />

## Usage

```ts
import { DataQna } from "yadl-ui";

const Example = () => {
  return (
      <DataQna />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
