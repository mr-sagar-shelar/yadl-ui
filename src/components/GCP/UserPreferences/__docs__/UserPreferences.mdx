import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as UserPreferences from "./UserPreferences.stories.tsx";

<Meta of={UserPreferences} title="GCP/UserPreferences" />

# UserPreferences

UserPreferences component with different props.

#### Example

<Canvas of={UserPreferences.Primary} />

## Usage

```ts
import { UserPreferences } from "yadl-ui";

const Example = () => {
  return (
      <UserPreferences />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
