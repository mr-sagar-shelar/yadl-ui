import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as TrafficDirector from "./TrafficDirector.stories.tsx";

<Meta of={TrafficDirector} title="GCP/TrafficDirector" />

# TrafficDirector

TrafficDirector component with different props.

#### Example

<Canvas of={TrafficDirector.Primary} />

## Usage

```ts
import { TrafficDirector } from "yadl-ui";

const Example = () => {
  return (
      <TrafficDirector />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
