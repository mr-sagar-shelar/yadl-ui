import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudMediaEdge from "./CloudMediaEdge.stories.tsx";

<Meta of={CloudMediaEdge} title="GCP/CloudMediaEdge" />

# CloudMediaEdge

CloudMediaEdge component with different props.

#### Example

<Canvas of={CloudMediaEdge.Primary} />

## Usage

```ts
import { CloudMediaEdge } from "yadl-ui";

const Example = () => {
  return (
      <CloudMediaEdge />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
