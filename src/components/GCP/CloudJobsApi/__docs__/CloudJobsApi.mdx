import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudJobsApi from "./CloudJobsApi.stories.tsx";

<Meta of={CloudJobsApi} title="GCP/CloudJobsApi" />

# CloudJobsApi

CloudJobsApi component with different props.

#### Example

<Canvas of={CloudJobsApi.Primary} />

## Usage

```ts
import { CloudJobsApi } from "yadl-ui";

const Example = () => {
  return (
      <CloudJobsApi />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
