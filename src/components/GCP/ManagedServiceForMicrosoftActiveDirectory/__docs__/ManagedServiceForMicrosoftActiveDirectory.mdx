import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ManagedServiceForMicrosoftActiveDirectory from "./ManagedServiceForMicrosoftActiveDirectory.stories.tsx";

<Meta of={ManagedServiceForMicrosoftActiveDirectory} title="GCP/ManagedServiceForMicrosoftActiveDirectory" />

# ManagedServiceForMicrosoftActiveDirectory

ManagedServiceForMicrosoftActiveDirectory component with different props.

#### Example

<Canvas of={ManagedServiceForMicrosoftActiveDirectory.Primary} />

## Usage

```ts
import { ManagedServiceForMicrosoftActiveDirectory } from "yadl-ui";

const Example = () => {
  return (
      <ManagedServiceForMicrosoftActiveDirectory />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
