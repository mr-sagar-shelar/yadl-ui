import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ReleaseNotes from "./ReleaseNotes.stories.tsx";

<Meta of={ReleaseNotes} title="GCP/ReleaseNotes" />

# ReleaseNotes

ReleaseNotes component with different props.

#### Example

<Canvas of={ReleaseNotes.Primary} />

## Usage

```ts
import { ReleaseNotes } from "yadl-ui";

const Example = () => {
  return (
      <ReleaseNotes />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
