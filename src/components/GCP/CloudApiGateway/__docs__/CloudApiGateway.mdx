import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudApiGateway from "./CloudApiGateway.stories.tsx";

<Meta of={CloudApiGateway} title="GCP/CloudApiGateway" />

# CloudApiGateway

CloudApiGateway component with different props.

#### Example

<Canvas of={CloudApiGateway.Primary} />

## Usage

```ts
import { CloudApiGateway } from "yadl-ui";

const Example = () => {
  return (
      <CloudApiGateway />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
