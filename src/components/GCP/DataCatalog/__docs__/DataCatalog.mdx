import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DataCatalog from "./DataCatalog.stories.tsx";

<Meta of={DataCatalog} title="GCP/DataCatalog" />

# DataCatalog

DataCatalog component with different props.

#### Example

<Canvas of={DataCatalog.Primary} />

## Usage

```ts
import { DataCatalog } from "yadl-ui";

const Example = () => {
  return (
      <DataCatalog />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
