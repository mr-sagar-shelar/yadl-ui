import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Connectors from "./Connectors.stories.tsx";

<Meta of={Connectors} title="GCP/Connectors" />

# Connectors

Connectors component with different props.

#### Example

<Canvas of={Connectors.Primary} />

## Usage

```ts
import { Connectors } from "yadl-ui";

const Example = () => {
  return (
      <Connectors />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
