import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudSecurityScanner from "./CloudSecurityScanner.stories.tsx";

<Meta of={CloudSecurityScanner} title="GCP/CloudSecurityScanner" />

# CloudSecurityScanner

CloudSecurityScanner component with different props.

#### Example

<Canvas of={CloudSecurityScanner.Primary} />

## Usage

```ts
import { CloudSecurityScanner } from "yadl-ui";

const Example = () => {
  return (
      <CloudSecurityScanner />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
