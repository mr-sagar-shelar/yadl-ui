import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as WorkloadIdentityPool from "./WorkloadIdentityPool.stories.tsx";

<Meta of={WorkloadIdentityPool} title="GCP/WorkloadIdentityPool" />

# WorkloadIdentityPool

WorkloadIdentityPool component with different props.

#### Example

<Canvas of={WorkloadIdentityPool.Primary} />

## Usage

```ts
import { WorkloadIdentityPool } from "yadl-ui";

const Example = () => {
  return (
      <WorkloadIdentityPool />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
