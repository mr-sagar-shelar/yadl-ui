import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudStorage from "./CloudStorage.stories.tsx";

<Meta of={CloudStorage} title="GCP/CloudStorage" />

# CloudStorage

CloudStorage component with different props.

#### Example

<Canvas of={CloudStorage.Primary} />

## Usage

```ts
import { CloudStorage } from "yadl-ui";

const Example = () => {
  return (
      <CloudStorage />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
