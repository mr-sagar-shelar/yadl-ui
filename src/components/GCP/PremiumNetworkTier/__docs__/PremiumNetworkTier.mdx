import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PremiumNetworkTier from "./PremiumNetworkTier.stories.tsx";

<Meta of={PremiumNetworkTier} title="GCP/PremiumNetworkTier" />

# PremiumNetworkTier

PremiumNetworkTier component with different props.

#### Example

<Canvas of={PremiumNetworkTier.Primary} />

## Usage

```ts
import { PremiumNetworkTier } from "yadl-ui";

const Example = () => {
  return (
      <PremiumNetworkTier />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
