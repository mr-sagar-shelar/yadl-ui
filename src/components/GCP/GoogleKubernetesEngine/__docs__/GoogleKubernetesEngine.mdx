import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as GoogleKubernetesEngine from "./GoogleKubernetesEngine.stories.tsx";

<Meta of={GoogleKubernetesEngine} title="GCP/GoogleKubernetesEngine" />

# GoogleKubernetesEngine

GoogleKubernetesEngine component with different props.

#### Example

<Canvas of={GoogleKubernetesEngine.Primary} />

## Usage

```ts
import { GoogleKubernetesEngine } from "yadl-ui";

const Example = () => {
  return (
      <GoogleKubernetesEngine />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
