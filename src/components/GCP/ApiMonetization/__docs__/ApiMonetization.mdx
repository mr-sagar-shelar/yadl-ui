import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ApiMonetization from "./ApiMonetization.stories.tsx";

<Meta of={ApiMonetization} title="GCP/ApiMonetization" />

# ApiMonetization

ApiMonetization component with different props.

#### Example

<Canvas of={ApiMonetization.Primary} />

## Usage

```ts
import { ApiMonetization } from "yadl-ui";

const Example = () => {
  return (
      <ApiMonetization />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
