import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as DataLabeling from "./DataLabeling.stories.tsx";

<Meta of={DataLabeling} title="GCP/DataLabeling" />

# DataLabeling

DataLabeling component with different props.

#### Example

<Canvas of={DataLabeling.Primary} />

## Usage

```ts
import { DataLabeling } from "yadl-ui";

const Example = () => {
  return (
      <DataLabeling />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
