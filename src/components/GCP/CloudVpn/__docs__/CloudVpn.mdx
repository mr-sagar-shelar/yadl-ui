import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudVpn from "./CloudVpn.stories.tsx";

<Meta of={CloudVpn} title="GCP/CloudVpn" />

# CloudVpn

CloudVpn component with different props.

#### Example

<Canvas of={CloudVpn.Primary} />

## Usage

```ts
import { CloudVpn } from "yadl-ui";

const Example = () => {
  return (
      <CloudVpn />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
