import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as NetworkIntelligenceCenter from "./NetworkIntelligenceCenter.stories.tsx";

<Meta of={NetworkIntelligenceCenter} title="GCP/NetworkIntelligenceCenter" />

# NetworkIntelligenceCenter

NetworkIntelligenceCenter component with different props.

#### Example

<Canvas of={NetworkIntelligenceCenter.Primary} />

## Usage

```ts
import { NetworkIntelligenceCenter } from "yadl-ui";

const Example = () => {
  return (
      <NetworkIntelligenceCenter />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
