import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Workflows from "./Workflows.stories.tsx";

<Meta of={Workflows} title="GCP/Workflows" />

# Workflows

Workflows component with different props.

#### Example

<Canvas of={Workflows.Primary} />

## Usage

```ts
import { Workflows } from "yadl-ui";

const Example = () => {
  return (
      <Workflows />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
