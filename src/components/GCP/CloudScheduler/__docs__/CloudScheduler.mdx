import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudScheduler from "./CloudScheduler.stories.tsx";

<Meta of={CloudScheduler} title="GCP/CloudScheduler" />

# CloudScheduler

CloudScheduler component with different props.

#### Example

<Canvas of={CloudScheduler.Primary} />

## Usage

```ts
import { CloudScheduler } from "yadl-ui";

const Example = () => {
  return (
      <CloudScheduler />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
