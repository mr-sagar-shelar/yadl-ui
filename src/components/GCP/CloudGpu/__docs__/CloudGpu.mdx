import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudGpu from "./CloudGpu.stories.tsx";

<Meta of={CloudGpu} title="GCP/CloudGpu" />

# CloudGpu

CloudGpu component with different props.

#### Example

<Canvas of={CloudGpu.Primary} />

## Usage

```ts
import { CloudGpu } from "yadl-ui";

const Example = () => {
  return (
      <CloudGpu />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
