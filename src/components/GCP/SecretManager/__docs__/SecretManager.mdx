import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as SecretManager from "./SecretManager.stories.tsx";

<Meta of={SecretManager} title="GCP/SecretManager" />

# SecretManager

SecretManager component with different props.

#### Example

<Canvas of={SecretManager.Primary} />

## Usage

```ts
import { SecretManager } from "yadl-ui";

const Example = () => {
  return (
      <SecretManager />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
