import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AnthosServiceMesh from "./AnthosServiceMesh.stories.tsx";

<Meta of={AnthosServiceMesh} title="GCP/AnthosServiceMesh" />

# AnthosServiceMesh

AnthosServiceMesh component with different props.

#### Example

<Canvas of={AnthosServiceMesh.Primary} />

## Usage

```ts
import { AnthosServiceMesh } from "yadl-ui";

const Example = () => {
  return (
      <AnthosServiceMesh />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
