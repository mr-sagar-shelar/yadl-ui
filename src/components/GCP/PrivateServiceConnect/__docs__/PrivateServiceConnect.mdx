import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PrivateServiceConnect from "./PrivateServiceConnect.stories.tsx";

<Meta of={PrivateServiceConnect} title="GCP/PrivateServiceConnect" />

# PrivateServiceConnect

PrivateServiceConnect component with different props.

#### Example

<Canvas of={PrivateServiceConnect.Primary} />

## Usage

```ts
import { PrivateServiceConnect } from "yadl-ui";

const Example = () => {
  return (
      <PrivateServiceConnect />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
