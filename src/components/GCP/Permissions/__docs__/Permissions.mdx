import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Permissions from "./Permissions.stories.tsx";

<Meta of={Permissions} title="GCP/Permissions" />

# Permissions

Permissions component with different props.

#### Example

<Canvas of={Permissions.Primary} />

## Usage

```ts
import { Permissions } from "yadl-ui";

const Example = () => {
  return (
      <Permissions />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
