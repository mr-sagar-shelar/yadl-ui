import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as RecommendationsAi from "./RecommendationsAi.stories.tsx";

<Meta of={RecommendationsAi} title="GCP/RecommendationsAi" />

# RecommendationsAi

RecommendationsAi component with different props.

#### Example

<Canvas of={RecommendationsAi.Primary} />

## Usage

```ts
import { RecommendationsAi } from "yadl-ui";

const Example = () => {
  return (
      <RecommendationsAi />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
