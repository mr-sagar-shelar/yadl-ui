import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Dialogflow from "./Dialogflow.stories.tsx";

<Meta of={Dialogflow} title="GCP/Dialogflow" />

# Dialogflow

Dialogflow component with different props.

#### Example

<Canvas of={Dialogflow.Primary} />

## Usage

```ts
import { Dialogflow } from "yadl-ui";

const Example = () => {
  return (
      <Dialogflow />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
