import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CloudEndpoints from "./CloudEndpoints.stories.tsx";

<Meta of={CloudEndpoints} title="GCP/CloudEndpoints" />

# CloudEndpoints

CloudEndpoints component with different props.

#### Example

<Canvas of={CloudEndpoints.Primary} />

## Usage

```ts
import { CloudEndpoints } from "yadl-ui";

const Example = () => {
  return (
      <CloudEndpoints />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
