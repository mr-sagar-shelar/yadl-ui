import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VideoIntelligenceApi from "./VideoIntelligenceApi.stories.tsx";

<Meta of={VideoIntelligenceApi} title="GCP/VideoIntelligenceApi" />

# VideoIntelligenceApi

VideoIntelligenceApi component with different props.

#### Example

<Canvas of={VideoIntelligenceApi.Primary} />

## Usage

```ts
import { VideoIntelligenceApi } from "yadl-ui";

const Example = () => {
  return (
      <VideoIntelligenceApi />
  );
};

export default Example;
```

#### Credits
[GCP](https://cloud.google.com/icons)
