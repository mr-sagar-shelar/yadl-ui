import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VisualStudio from "./VisualStudio.stories.tsx";

<Meta of={VisualStudio} title="SkillSet/VisualStudio" />

# VisualStudio

VisualStudio component with different props.

#### Example

<Canvas of={VisualStudio.Primary} />

## Usage

```ts
import { VisualStudio } from "yadl-ui";

const Example = () => {
  return (
      <VisualStudio />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
