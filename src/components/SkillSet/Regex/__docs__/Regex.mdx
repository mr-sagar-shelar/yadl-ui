import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Regex from "./Regex.stories.tsx";

<Meta of={Regex} title="SkillSet/Regex" />

# Regex

Regex component with different props.

#### Example

<Canvas of={Regex.Primary} />

## Usage

```ts
import { Regex } from "yadl-ui";

const Example = () => {
  return (
      <Regex />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
