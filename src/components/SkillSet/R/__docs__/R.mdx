import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as R from "./R.stories.tsx";

<Meta of={R} title="SkillSet/R" />

# R

R component with different props.

#### Example

<Canvas of={R.Primary} />

## Usage

```ts
import { R } from "yadl-ui";

const Example = () => {
  return (
      <R />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
