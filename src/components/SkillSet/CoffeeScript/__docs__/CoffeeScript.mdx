import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CoffeeScript from "./CoffeeScript.stories.tsx";

<Meta of={CoffeeScript} title="SkillSet/CoffeeScript" />

# CoffeeScript

CoffeeScript component with different props.

#### Example

<Canvas of={CoffeeScript.Primary} />

## Usage

```ts
import { CoffeeScript } from "yadl-ui";

const Example = () => {
  return (
      <CoffeeScript />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
