import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Ansible from "./Ansible.stories.tsx";

<Meta of={Ansible} title="SkillSet/Ansible" />

# Ansible

Ansible component with different props.

#### Example

<Canvas of={Ansible.Primary} />

## Usage

```ts
import { Ansible } from "yadl-ui";

const Example = () => {
  return (
      <Ansible />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
