import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Git from "./Git.stories.tsx";

<Meta of={Git} title="SkillSet/Git" />

# Git

Git component with different props.

#### Example

<Canvas of={Git.Primary} />

## Usage

```ts
import { Git } from "yadl-ui";

const Example = () => {
  return (
      <Git />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
