import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Docker from "./Docker.stories.tsx";

<Meta of={Docker} title="SkillSet/Docker" />

# Docker

Docker component with different props.

#### Example

<Canvas of={Docker.Primary} />

## Usage

```ts
import { Docker } from "yadl-ui";

const Example = () => {
  return (
      <Docker />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
