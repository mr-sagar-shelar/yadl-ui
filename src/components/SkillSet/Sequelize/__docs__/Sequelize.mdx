import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Sequelize from "./Sequelize.stories.tsx";

<Meta of={Sequelize} title="SkillSet/Sequelize" />

# Sequelize

Sequelize component with different props.

#### Example

<Canvas of={Sequelize.Primary} />

## Usage

```ts
import { Sequelize } from "yadl-ui";

const Example = () => {
  return (
      <Sequelize />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
