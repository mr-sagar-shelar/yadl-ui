import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ExpressJS from "./ExpressJS.stories.tsx";

<Meta of={ExpressJS} title="SkillSet/ExpressJS" />

# ExpressJS

ExpressJS component with different props.

#### Example

<Canvas of={ExpressJS.Primary} />

## Usage

```ts
import { ExpressJS } from "yadl-ui";

const Example = () => {
  return (
      <ExpressJS />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
