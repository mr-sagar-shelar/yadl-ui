import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Sass from "./Sass.stories.tsx";

<Meta of={Sass} title="SkillSet/Sass" />

# Sass

Sass component with different props.

#### Example

<Canvas of={Sass.Primary} />

## Usage

```ts
import { Sass } from "yadl-ui";

const Example = () => {
  return (
      <Sass />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
