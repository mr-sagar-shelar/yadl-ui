import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as WebStorm from "./WebStorm.stories.tsx";

<Meta of={WebStorm} title="SkillSet/WebStorm" />

# WebStorm

WebStorm component with different props.

#### Example

<Canvas of={WebStorm.Primary} />

## Usage

```ts
import { WebStorm } from "yadl-ui";

const Example = () => {
  return (
      <WebStorm />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
