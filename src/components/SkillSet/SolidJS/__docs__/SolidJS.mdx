import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as SolidJS from "./SolidJS.stories.tsx";

<Meta of={SolidJS} title="SkillSet/SolidJS" />

# SolidJS

SolidJS component with different props.

#### Example

<Canvas of={SolidJS.Primary} />

## Usage

```ts
import { SolidJS } from "yadl-ui";

const Example = () => {
  return (
      <SolidJS />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
