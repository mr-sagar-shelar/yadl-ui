import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as BitBucket from "./BitBucket.stories.tsx";

<Meta of={BitBucket} title="SkillSet/BitBucket" />

# BitBucket

BitBucket component with different props.

#### Example

<Canvas of={BitBucket.Primary} />

## Usage

```ts
import { BitBucket } from "yadl-ui";

const Example = () => {
  return (
      <BitBucket />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
