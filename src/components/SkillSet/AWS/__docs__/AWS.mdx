import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AWS from "./AWS.stories.tsx";

<Meta of={AWS} title="SkillSet/AWS" />

# AWS

AWS component with different props.

#### Example

<Canvas of={AWS.Primary} />

## Usage

```ts
import { AWS } from "yadl-ui";

const Example = () => {
  return (
      <AWS />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
