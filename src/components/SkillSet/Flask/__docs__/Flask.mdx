import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Flask from "./Flask.stories.tsx";

<Meta of={Flask} title="SkillSet/Flask" />

# Flask

Flask component with different props.

#### Example

<Canvas of={Flask.Primary} />

## Usage

```ts
import { Flask } from "yadl-ui";

const Example = () => {
  return (
      <Flask />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
