import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as TypeScript from "./TypeScript.stories.tsx";

<Meta of={TypeScript} title="SkillSet/TypeScript" />

# TypeScript

TypeScript component with different props.

#### Example

<Canvas of={TypeScript.Primary} />

## Usage

```ts
import { TypeScript } from "yadl-ui";

const Example = () => {
  return (
      <TypeScript />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
