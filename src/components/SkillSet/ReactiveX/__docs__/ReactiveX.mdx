import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as ReactiveX from "./ReactiveX.stories.tsx";

<Meta of={ReactiveX} title="SkillSet/ReactiveX" />

# ReactiveX

ReactiveX component with different props.

#### Example

<Canvas of={ReactiveX.Primary} />

## Usage

```ts
import { ReactiveX } from "yadl-ui";

const Example = () => {
  return (
      <ReactiveX />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
