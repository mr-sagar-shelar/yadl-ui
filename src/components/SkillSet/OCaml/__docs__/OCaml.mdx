import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as OCaml from "./OCaml.stories.tsx";

<Meta of={OCaml} title="SkillSet/OCaml" />

# OCaml

OCaml component with different props.

#### Example

<Canvas of={OCaml.Primary} />

## Usage

```ts
import { OCaml } from "yadl-ui";

const Example = () => {
  return (
      <OCaml />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
