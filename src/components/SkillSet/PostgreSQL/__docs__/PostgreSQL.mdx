import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PostgreSQL from "./PostgreSQL.stories.tsx";

<Meta of={PostgreSQL} title="SkillSet/PostgreSQL" />

# PostgreSQL

PostgreSQL component with different props.

#### Example

<Canvas of={PostgreSQL.Primary} />

## Usage

```ts
import { PostgreSQL } from "yadl-ui";

const Example = () => {
  return (
      <PostgreSQL />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
