import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as TensorFlow from "./TensorFlow.stories.tsx";

<Meta of={TensorFlow} title="SkillSet/TensorFlow" />

# TensorFlow

TensorFlow component with different props.

#### Example

<Canvas of={TensorFlow.Primary} />

## Usage

```ts
import { TensorFlow } from "yadl-ui";

const Example = () => {
  return (
      <TensorFlow />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
