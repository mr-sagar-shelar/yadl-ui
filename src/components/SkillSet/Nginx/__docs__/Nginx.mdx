import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Nginx from "./Nginx.stories.tsx";

<Meta of={Nginx} title="SkillSet/Nginx" />

# Nginx

Nginx component with different props.

#### Example

<Canvas of={Nginx.Primary} />

## Usage

```ts
import { Nginx } from "yadl-ui";

const Example = () => {
  return (
      <Nginx />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
