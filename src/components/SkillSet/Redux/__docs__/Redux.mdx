import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Redux from "./Redux.stories.tsx";

<Meta of={Redux} title="SkillSet/Redux" />

# Redux

Redux component with different props.

#### Example

<Canvas of={Redux.Primary} />

## Usage

```ts
import { Redux } from "yadl-ui";

const Example = () => {
  return (
      <Redux />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
