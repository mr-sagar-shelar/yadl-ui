import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VIM from "./VIM.stories.tsx";

<Meta of={VIM} title="SkillSet/VIM" />

# VIM

VIM component with different props.

#### Example

<Canvas of={VIM.Primary} />

## Usage

```ts
import { VIM } from "yadl-ui";

const Example = () => {
  return (
      <VIM />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
