import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as VueJS from "./VueJS.stories.tsx";

<Meta of={VueJS} title="SkillSet/VueJS" />

# VueJS

VueJS component with different props.

#### Example

<Canvas of={VueJS.Primary} />

## Usage

```ts
import { VueJS } from "yadl-ui";

const Example = () => {
  return (
      <VueJS />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
