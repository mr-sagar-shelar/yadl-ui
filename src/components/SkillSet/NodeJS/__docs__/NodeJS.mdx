import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as NodeJS from "./NodeJS.stories.tsx";

<Meta of={NodeJS} title="SkillSet/NodeJS" />

# NodeJS

NodeJS component with different props.

#### Example

<Canvas of={NodeJS.Primary} />

## Usage

```ts
import { NodeJS } from "yadl-ui";

const Example = () => {
  return (
      <NodeJS />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
