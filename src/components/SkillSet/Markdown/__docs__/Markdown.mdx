import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Markdown from "./Markdown.stories.tsx";

<Meta of={Markdown} title="SkillSet/Markdown" />

# Markdown

Markdown component with different props.

#### Example

<Canvas of={Markdown.Primary} />

## Usage

```ts
import { Markdown } from "yadl-ui";

const Example = () => {
  return (
      <Markdown />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
