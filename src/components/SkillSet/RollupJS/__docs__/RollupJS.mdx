import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as RollupJS from "./RollupJS.stories.tsx";

<Meta of={RollupJS} title="SkillSet/RollupJS" />

# RollupJS

RollupJS component with different props.

#### Example

<Canvas of={RollupJS.Primary} />

## Usage

```ts
import { RollupJS } from "yadl-ui";

const Example = () => {
  return (
      <RollupJS />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
