import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Prometheus from "./Prometheus.stories.tsx";

<Meta of={Prometheus} title="SkillSet/Prometheus" />

# Prometheus

Prometheus component with different props.

#### Example

<Canvas of={Prometheus.Primary} />

## Usage

```ts
import { Prometheus } from "yadl-ui";

const Example = () => {
  return (
      <Prometheus />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
