import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as OpenShift from "./OpenShift.stories.tsx";

<Meta of={OpenShift} title="SkillSet/OpenShift" />

# OpenShift

OpenShift component with different props.

#### Example

<Canvas of={OpenShift.Primary} />

## Usage

```ts
import { OpenShift } from "yadl-ui";

const Example = () => {
  return (
      <OpenShift />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
