import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Replit from "./Replit.stories.tsx";

<Meta of={Replit} title="SkillSet/Replit" />

# Replit

Replit component with different props.

#### Example

<Canvas of={Replit.Primary} />

## Usage

```ts
import { Replit } from "yadl-ui";

const Example = () => {
  return (
      <Replit />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
