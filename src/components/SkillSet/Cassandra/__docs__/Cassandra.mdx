import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Cassandra from "./Cassandra.stories.tsx";

<Meta of={Cassandra} title="SkillSet/Cassandra" />

# Cassandra

Cassandra component with different props.

#### Example

<Canvas of={Cassandra.Primary} />

## Usage

```ts
import { Cassandra } from "yadl-ui";

const Example = () => {
  return (
      <Cassandra />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
