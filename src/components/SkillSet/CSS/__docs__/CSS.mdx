import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CSS from "./CSS.stories.tsx";

<Meta of={CSS} title="SkillSet/CSS" />

# CSS

CSS component with different props.

#### Example

<Canvas of={CSS.Primary} />

## Usage

```ts
import { CSS } from "yadl-ui";

const Example = () => {
  return (
      <CSS />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
