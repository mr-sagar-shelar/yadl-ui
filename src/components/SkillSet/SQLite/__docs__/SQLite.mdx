import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as SQLite from "./SQLite.stories.tsx";

<Meta of={SQLite} title="SkillSet/SQLite" />

# SQLite

SQLite component with different props.

#### Example

<Canvas of={SQLite.Primary} />

## Usage

```ts
import { SQLite } from "yadl-ui";

const Example = () => {
  return (
      <SQLite />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
