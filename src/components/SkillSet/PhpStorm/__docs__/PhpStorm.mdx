import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PhpStorm from "./PhpStorm.stories.tsx";

<Meta of={PhpStorm} title="SkillSet/PhpStorm" />

# PhpStorm

PhpStorm component with different props.

#### Example

<Canvas of={PhpStorm.Primary} />

## Usage

```ts
import { PhpStorm } from "yadl-ui";

const Example = () => {
  return (
      <PhpStorm />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
