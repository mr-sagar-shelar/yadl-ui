import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Jenkins from "./Jenkins.stories.tsx";

<Meta of={Jenkins} title="SkillSet/Jenkins" />

# Jenkins

Jenkins component with different props.

#### Example

<Canvas of={Jenkins.Primary} />

## Usage

```ts
import { Jenkins } from "yadl-ui";

const Example = () => {
  return (
      <Jenkins />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
