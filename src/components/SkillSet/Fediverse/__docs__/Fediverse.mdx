import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Fediverse from "./Fediverse.stories.tsx";

<Meta of={Fediverse} title="SkillSet/Fediverse" />

# Fediverse

Fediverse component with different props.

#### Example

<Canvas of={Fediverse.Primary} />

## Usage

```ts
import { Fediverse } from "yadl-ui";

const Example = () => {
  return (
      <Fediverse />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
