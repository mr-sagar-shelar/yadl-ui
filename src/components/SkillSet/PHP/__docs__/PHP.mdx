import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as PHP from "./PHP.stories.tsx";

<Meta of={PHP} title="SkillSet/PHP" />

# PHP

PHP component with different props.

#### Example

<Canvas of={PHP.Primary} />

## Usage

```ts
import { PHP } from "yadl-ui";

const Example = () => {
  return (
      <PHP />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
