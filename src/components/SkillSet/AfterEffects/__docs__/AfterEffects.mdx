import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as AfterEffects from "./AfterEffects.stories.tsx";

<Meta of={AfterEffects} title="SkillSet/AfterEffects" />

# AfterEffects

AfterEffects component with different props.

#### Example

<Canvas of={AfterEffects.Primary} />

## Usage

```ts
import { AfterEffects } from "yadl-ui";

const Example = () => {
  return (
      <AfterEffects />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
