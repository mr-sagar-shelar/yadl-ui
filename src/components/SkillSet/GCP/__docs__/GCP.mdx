import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as GCP from "./GCP.stories.tsx";

<Meta of={GCP} title="SkillSet/GCP" />

# GCP

GCP component with different props.

#### Example

<Canvas of={GCP.Primary} />

## Usage

```ts
import { GCP } from "yadl-ui";

const Example = () => {
  return (
      <GCP />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
