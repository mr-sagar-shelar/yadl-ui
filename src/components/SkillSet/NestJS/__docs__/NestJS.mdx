import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as NestJS from "./NestJS.stories.tsx";

<Meta of={NestJS} title="SkillSet/NestJS" />

# NestJS

NestJS component with different props.

#### Example

<Canvas of={NestJS.Primary} />

## Usage

```ts
import { NestJS } from "yadl-ui";

const Example = () => {
  return (
      <NestJS />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
