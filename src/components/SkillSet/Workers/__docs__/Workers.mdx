import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Workers from "./Workers.stories.tsx";

<Meta of={Workers} title="SkillSet/Workers" />

# Workers

Workers component with different props.

#### Example

<Canvas of={Workers.Primary} />

## Usage

```ts
import { Workers } from "yadl-ui";

const Example = () => {
  return (
      <Workers />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
