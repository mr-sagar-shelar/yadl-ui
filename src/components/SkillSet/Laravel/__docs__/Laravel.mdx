import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Laravel from "./Laravel.stories.tsx";

<Meta of={Laravel} title="SkillSet/Laravel" />

# Laravel

Laravel component with different props.

#### Example

<Canvas of={Laravel.Primary} />

## Usage

```ts
import { Laravel } from "yadl-ui";

const Example = () => {
  return (
      <Laravel />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
