import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as HTML from "./HTML.stories.tsx";

<Meta of={HTML} title="SkillSet/HTML" />

# HTML

HTML component with different props.

#### Example

<Canvas of={HTML.Primary} />

## Usage

```ts
import { HTML } from "yadl-ui";

const Example = () => {
  return (
      <HTML />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
