import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as MongoDB from "./MongoDB.stories.tsx";

<Meta of={MongoDB} title="SkillSet/MongoDB" />

# MongoDB

MongoDB component with different props.

#### Example

<Canvas of={MongoDB.Primary} />

## Usage

```ts
import { MongoDB } from "yadl-ui";

const Example = () => {
  return (
      <MongoDB />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
