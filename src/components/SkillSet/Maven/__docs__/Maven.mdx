import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as Maven from "./Maven.stories.tsx";

<Meta of={Maven} title="SkillSet/Maven" />

# Maven

Maven component with different props.

#### Example

<Canvas of={Maven.Primary} />

## Usage

```ts
import { Maven } from "yadl-ui";

const Example = () => {
  return (
      <Maven />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
