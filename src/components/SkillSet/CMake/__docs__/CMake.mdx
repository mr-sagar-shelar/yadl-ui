import { Canvas, Meta } from "@storybook/blocks";
import Example from "./Example.tsx";
import * as CMake from "./CMake.stories.tsx";

<Meta of={CMake} title="SkillSet/CMake" />

# CMake

CMake component with different props.

#### Example

<Canvas of={CMake.Primary} />

## Usage

```ts
import { CMake } from "yadl-ui";

const Example = () => {
  return (
      <CMake />
  );
};

export default Example;
```

#### Credits
[SkillSet](https://skillicons.dev)
